# scripts/

Node scripts for running and verifying the game. None of them are part of the build.

| File | Role |
|---|---|
| `verify.mjs` | **Verification runner** — `npm run verify` (smokes mapped to the folders changed in the working tree) / `npm run verify:all` (everything, before a merge). Runs typecheck (client + server), `net:selftest` and optionally `vite build` in parallel, starts vite (5273) and the relay (8787) when they are not up, restarts the relay before `e2e-mp` (stale public lobbies hijack quick match), runs the smoke scripts in 4 staggered lanes (each owns a headless Chrome on the GPU), then `e2e-mp` alone. Prints one line per script + the `FAIL` lines, full output in `logs/<name>.log`, records `logs/last-run.json` for `--rerun-failed`, and ends with a `docs line:` to paste into `docs/VERIFICATION.md`. `--list` shows the folder → script map (`SMOKES` table at the top — register new smokes there). |
| `dev-all.mjs` | `npm run dev:all`: relay + vite together with prefixed output, Ctrl+C stops both. |
| `e2e-multiplayer.mjs` | `npm run e2e:mp` (**156 checks**, 124 before Phase 11 — the new ones are the 소셜 group (아이디, friend request → accept → mutual presence, whisper both ways, mutual removal) and the 목표 행성 group (host `setLobbyPlanet` → guest mirror, guest refused, unknown id dropped, `net:gameStarting.planet` / `game:newMission.planet` / `ctx.missionPlanet` at the start, none for a training); both groups skip themselves with a logged note on a relay without the social store / `lobby:planet`): two headless Chrome instances through a running relay + vite (hub → quick match → docking → pods → mission → pickups → reconnect → abort). Phase 7: server profile (`ctx.net.profile.available`, credits tx round trip + overdraft refusal), `inMission` after the raid start, `net:peerSuspended` around a squadmate drop, host held offline > `NET_HOST_MIGRATE_DELAY_MS` → client takes over (`net:hostChanged`, `tookOver`), returning host demoted + `flow rejoined`, the new host aborts. **Phase 9** adds four groups: the **overcharge beam** (A channels Q with a faked pointer lock at B 6 m ahead → B receives `imp beam {target:B}`, plays `overcharge_beam` and `RemoteImplants.debugBeam(A)` reports `on` with that target; keyup turns both off), **container authority** (B searches a crate, `quickMove` → `'pending'` + `pendingTakeUids`, the host confirms with `cont taken`, B's bag grows and the queue empties; a second request for an index the host already gave out comes back `cont denied`), **delta snapshots** (the host / client enemy counts are compared again after a 3 s idle and once more after the host migration), and a **training join** (the non-host starts one, the host joins, each exits individually and the server resets the lobby). Needs a freshly started relay — use the runner. Against a private relay: `PORT=8797 npm run server` + `VITE_WS_URL=ws://127.0.0.1:8797/ws npx vite --port 5311`, then pass `http://localhost:5311/`. |
| `smoke-weapons.mjs` | Weapon package: grades, durability, ammo v2, 3 slots, sockets, bags, workbench repair + **Phase 7** `ctx.weapons.remoteState` (idle, attachments list after `attachToWeapon` / stable array instance / new instance on change / empty after detach, T tap → `heldItemId`, LMB hold → `throwing`, R → `cooking`, release / 1 clear) and a visual-only replica grenade damaging the local player without `grenade:exploded`; **Phase 9** barrier / blockers (a line-of-sight query leaves the shield hp untouched and emits no `implant:barrierHit`, a real hitscan that ends on the barrier bills it exactly once, a solid deployable takes none) and status attacker (화염방사기 / 전격총 pass `ctx.net.localId ?? 'local'` into `applyStatus`) (93 checks). |
| `smoke-phase2.mjs` | Downed / bleed / give-up → 레이드 실패 (Phase 7: a solo death fails the raid, auto return + starter kit), quick wheel, stim in hand, grenade cooking; **Phase 9** the give-up hold reports `player:giveUpProgress` (rising `t` while Space is held, a single `-1` on release and when the downed state ends); **2026-09-07** the bottom-left 회복약 / 수류탄 pills are gone (asserted absent) (49). Parks the `vite-hmr` socket. |
| `smoke-quickslots.mjs` | Quick-slot model + real-mouse drag onto the compass rose; **2026-09-07**: the drag ghost must ride **centred on the pointer** (`.inv-ghost`'s standalone `scale:` used to multiply the translate) (46). |
| `smoke-phase3.mjs` | Ship calls: wheel / arm, top-view targeting, airstrike / structures / supply / laser, HUD layers (32). |
| `smoke-stratagems.mjs` | Stratagems folder smoke: G tap / wheel, ground targeting, airstrike / structure drop via `debugCall`, top-view laser, cleanup — plus **Phase 9 late-join sync** (a synthetic `strat sync` through the real `NetSystem` handlers rebuilds the calls, their structures and the synced structure hp; an already-landed call is fast-forwarded silently; a `stratq sync` / `flow rejoined` from a peer makes the host answer with its own call list) (70; registers the system itself if `main.ts` has not). |
| `smoke-phase4.mjs` | Rogues, boss, artillery interception, toxic, behemoth armour (4.8 m since Phase 7), factions, corpses (46 incl. the Phase 6 status block). |
| `smoke-rogue-v2.mjs` | Phase 7 §6 enemies: behemoth at `BEHEMOTH_SCALE` 3 (stats / rig / plate rays), rogue cover that must block LOS (crouched eye → chest raycast re-validated per pick), magazine of 12 → ≈ 2 s reload (hint 12, `reload` audio, pose, mag refill), grenade after a 3 s LOS hold (hint 13, hand sphere, flight, fuse, falloff damage, cooldown), `setAuthority(false / true)` round-trip (count kept, replica hit path, AI resumes, id headroom), training world = no spawning (52). Parks the `vite-hmr` socket. |
| `smoke-tactical.mjs` | Tactical kit: implants, gadgets, armor / weight, melee, roll, gather nodes, crafting, progression; **Phase 9** the jump pad launches a player **exactly once** inside `JUMP_PAD_RETRIGGER_S` (was 3 launches in 2.5 s) and the barrier split — `raycastBarrier(fromEnemy=false)` never blocks, `fromEnemy=true` reports the local shield **without** touching its hp or emitting `implant:barrierHit`, and `damageBarrier(owner, point[, amount])` is what spends durability (a peer's barrier leaves the local hp alone) (64). |
| `smoke-controls-hub.mjs` | Controls diagram + rebinding, hub Tab ship screen (stash / repair / screen tabs), terminal, implant gauge; **Phase 8 UI pass**: the 전술 임플란트 popup is centred + fixed-size with the `장착 중` label under the description, `.inv-screen` has its own panel background, the 함선 tab is two columns with a locked room 1 and a sticky 시설 관리 (M) button, and hovering a `.item-chip[data-def-id]` shows / hides the `ui/hud/ItemTip` card; **2026-09-07**: the 캐릭터 tab is three columns (능력치 | 숙련도 | 전술 임플란트) and the implant list moved into a modeless picker — the 장착 칸 opens `.cs-imp-pop-embed`, a card equips and closes it, and every card selector is scoped to that picker because the standalone overlay's `SheetBody` exists at the same time; **2026-09-07 (2)**: the virtual cursor moves 1:1 with the raw delta (no acceleration curve) and a **denied** pointer-lock request arms the gesture retry — Escape never counts, any other key retries (85; `--shots` writes PNGs to `shots/`). |
| `smoke-inventory-p6.mjs` | Phase 6 inventory: 무한 상자 catalog (tabs / search / real-mouse drag into bag · stash · slot / double-click), stash size + reload persistence, `countDefAll` / `consumeDefAll`, `captureLoadout` / `applyLoadout`, bench craft panel (locked rows, discount, repair list) (63). |
| `smoke-loadout.mjs` | Phase 5 inventory: loadout persistence (`scav.loadout`: ship changes survive a reload, starter resets overwrite the save, `game:complete` saves), corp-shop access (`tryAddToStash` / `tryAddItemAnywhere` / `takeItem` / `findItemAnywhere`), `inventory:containerOpened {first}`, ship-screen 크레딧 readout + active 기업 tab, and the Phase 9 profile hook (the starter kit goes up as a `fresh` document and coming back inside the merged record neither re-applies nor re-saves it) (54). |
| `smoke-search.mjs` | Phase 7 inventory: Tarkov-style container search (hidden tiles, reveal order / timing, progress kept across close / reopen, 감정 speed, take-all gating), `canFit`, `captureRaidState` / `applyRaidState`, the server profile hooks (`profile.set` after every stash / loadout save — offline included — plus the real `net/ProfileSync` newest-wins merge driven directly) and host-authoritative container takes through the real `NetSystem` handlers (`contq take` → `cont taken` / `denied` / `sync`, host validation + broadcast) (59). |
| `smoke-progression.mjs` | Stat XP: `addStatXp` raise / clamp at STAT_MAX / lower to STAT_MIN / carry-over, `addSkillXpRaw` down to level 0, profile migration + reload persistence, character-sheet stat bars; Phase 7: 감정 XP from `container:itemRevealed`, training = `gun_*` only, server `progression` document (save → `profile.set`, `net:profileLoaded` replace + `progress:*` re-emit) through a fake `ctx.net.profile` (**Phase 9**: `set` is called offline too); Phase 9 housing multiplier — a fake `ctx.housing.getSkillGainMul` (사격장 × 서재 folded into one number) multiplies the XP `addSkillXp` awards (68). |
| `smoke-ui-p6.mjs` | Phase 6 HUD (synthetic bus events in a live solo mission): unique-weapon charge arc per kind / hidden at −1, weapon-panel `좌/우` mode lines, 🔥 전소 / ⚡ markers expiring on sim time, `MOVE CHEAT` tag, housing hint bar + key hints, room label fade; **Phase 9** the vitals 포기 bar (`player:giveUpProgress` shows / scales it, `-1` and leaving the downed state hide it) and the `TrainingPanel` (only while `ctx.missionMode === 'training'`, mode chip from `training:modeChanged`, 격추 score + bar from `training:scored`, 남은 시간 row, 최고 기록, `training:courseFinished` toast + pulse) (68). |
| `smoke-social.mjs` | **Phase 11 소셜 UI** (`src/ui`, no relay — `hud.debugSocial(snapshot, invites, mySquad)` installs a synthetic `SocialRef` and `hud.debugSocialLog` records what the UI asked it to do): the ESC re-layout (frame in the left half, `.pause-social` in the right half, unchanged 4-button set + `함선으로 귀환` still `display:none` in the ship), the offline fallback (`소셜 기능을 사용할 수 없습니다` and nothing else), the column (4 친구 / 5 최근 cards, 2 columns, `--rows` 3.50 / 5.50, `친구 n · 접속 m`, 분대원 hidden with no lobby, request card with 수락 / 거절, `AB3D-9KMN` formatting, `.is-offline`), the right-click menu (친구 → 같이 하기 · 귓속말하기 · 친구 삭제 / 최근 → 친구 추가, 같이 하기 greyed with 임무 중 · 오프라인 · 상대 분대가 가득 참, a disabled entry firing nothing, `requestFriend` / `playWith`, the 친구 삭제 confirm card with 취소 vs 삭제 → `removeFriend`, 수락 → `respondFriend`, Escape closing only the menu), the 설정 side panel (`.side`, 오디오 then 키 설정, a real `ControlsPanel` with lit keys + the mouse diagram + the `키 설정 변경` button, Escape closing it and leaving the pause menu), the 커뮤니티 thumbnail (32 / 28 px, the online count inside it, the red dot on / off, `.cheat-tag` pushed to 108 px) and panel (`COMMUNITY_BLOCKER` + software cursor, the same column reused, 내 아이디, Escape), the 분대 초대 stack (capped at 3, newest first, below the thumbnail, `P 홀드로 참여`, the gauge filling / resetting / accepting at `SQUAD_INVITE_HOLD_S`, `×` → `dismissInvite`), 귓속말 (chip, `.hud-bl.whispering`, `social.whisper` on Enter, no local echo, both `social:whisper` directions, clearing the target, the failure line without a mirror) and the result screens (planet line, `목표 미지정`, `다시 배치` carrying `ctx.missionPlanet`) plus the raid ESC (no social column, no 커뮤니티) (116). Parks the `vite-hmr` socket; the relay is optional. |
| `smoke-ui-p5.mjs` | Phase 5 HUD / menus (synthetic `meta:*` / `game:complete` / `game:over` events in a live solo mission, `ctx.meta` may be the skeleton): title `Lv. n` chip (`progress:levelUp` / `progress:loaded`), contract panel under the objective (`meta:contractProgress` alone opens it, pulse on sim time, `달성` badge, hidden on settle / abandon / abort), coalesced `±n 크레딧` chip, `<기업> 신뢰도 Lv.n`, 계약 성공 / 미완 · 계속 / 실패 · 진척 유지 안 됨 toasts keyed on `settlement.outcome` (+ no-`outcome` fallback), quest / contract / purchase / sale lines, result-screen XP block (count-up, `Lv. a → b`, `.up` + 레벨 업 badge + `.up-burst` at the boundary crossing with a single `level_up` audio, bar fractions, outcome contract lines on both screens, hidden without `rewards`), **Phase 7**: no `progress:levelUp` toast, 레이드 실패 death screen (`game:raidFailed`: title, no 부활, `12초 후 자동 귀환` ticking on sim time, Space ignored, reset on `game:abort`), suspended nameplate / squad row + 훈련장 / 임무 중 / 함선 badges with a `remotePlayers.debugSpawn` peer through `hud.debugRemotes`, `net:hostChanged` / `net:peerSuspended` / training chat + notification lines, training objective + `ui:objective` subText; **Phase 9**: the host-ghost state of a suspended member on both the nameplate and the squad row (`ghostState 1` + `ghostDownHp` → `.bleeding` red bar scaled to the down pool while the `연결 끊김` tag stays, a shrinking pool rescales both, `ghostState 2` → `사망` tag / `.dead` and no bar, back to `0` / `undefined` → plain `연결 끊김`); **2026-09-07** the bottom-left vitals are health + stamina only, with no `.pill` left (132). Parks the `vite-hmr` socket; needs the relay on 8787 (the hub's `ensureConnected` logs a console error otherwise). |
| `smoke-uniques.mjs` | Phase 6 unique weapons (equipped via `createItem` + `equip`, enemies via `debugSpawn`): flame cone damage / fuel drain / durability per second / 전소 after ~2.5 s / RMB jet, shock arc on two targets + `enemy:shocked` / RMB charge gauge → bolt for 6 cells, shuriken LMB / RMB fan / F tap swing / F hold 용검 (50 % stamina, view widen, 280 dmg, refusal toast), bow ADS zoom + straight arrow, bazooka blast + area damage + auto reload + airborne RMB rocket jump (`player:blastJump`, self damage 22, velocity.y up), minigun no shot before spin-up / spin gauge / 24 Hz fire / spin-down −1 / RMB pre-spin; **Phase 7** `remoteState.spraying` (flame on / off), `charging` (shock bolt charge on / off, minigun spin-up on / off), `heavy` (bazooka) (71). |
| `smoke-console.mjs` | Developer console: dev-host gating, ` toggle / blocker / Esc capture, suggestions + history, `/seed /move /movecheat /items /stat /skill /help /clear`, Home move cheat (63; parks the `vite-hmr` socket so other editors' saves cannot reload the page mid-run). |
| `smoke-housing.mjs` | Ship housing: fresh state, room purposes, placement rules (bounds / overlap / purpose), generator-gated facility upgrades, stash size, furniture craft / upgrade, range presets, housing-mode state, the 프리셋 panel, reload persistence + corrupt-save sanitising; Phase 7: `furn_sim_hub` craft / place, server `ship` document (save → `profile.set`, `net:profileLoaded` replace + `housing:loaded` re-emit + stash size); **Phase 8 UI pass**: the fresh state (방 1 = 작업실 with both benches placed), the room-1 lock in both directions, furniture migrated into storage by a corrupt/old save, and the 시설 관리 screen replacing the 방/시설 메뉴 (방 목록, 가구 목록, 용도 지정 picker with blocked purposes, `openRoomMenu` / `openFacilityMenu` redirecting); **Phase 9**: state v3 (fresh state carries empty `books` / `bookDex`), the 18-def catalogue with `furn_bookshelf` in the 서재 list only, and `ShipStore.upload()` calling `profile.set('ship')` **offline too** — the 서재 mechanics themselves are `smoke-library`'s (173). |
| `smoke-ship-rooms.mjs` | Personal-ship rooms + 3D housing mode: cockpit interactables, the removed door / facility consoles, room 1 = the built-in 작업실 with both benches placed, `currentRoom` / `hub:roomEntered`, door walk-through, terminal without a seed section, housing-mode camera / cursor / ghost / LMB place / X recover / Esc, **시설 관리 (M)**: the screen + `shipmanage` blocker on M, and Esc / C both releasing the blocker, restoring the controls and bringing the corner hint back without opening the pause menu, furniture mesh + collider + interactable, **ship computer** (Phase 5: `hub_computer` prompt / anchor, desk collider, `크레딧` line on the terminal screen + `meta:creditsChanged` refresh, E → `ui:corpToggled {open:true}` + terminal locked + Esc closes without opening the terminal — or the fallback warning toast while `ctx.meta` is a stub) (71). Falls back to faking `housing:*` events when `ctx.housing` is a stub; parks the vite HMR socket. |
| `smoke-ecology.mjs` | **Phase 11 행성 생태계** (world + enemies, single-player, no relay): the no-planet baseline (`ctx.world.planet` / `world:ready.planet` null, seeded biome, 34 herb nodes, cap `12 + 24 × threat`, the old ambient / wave type set and gates, 16 guards + boss), then each of the five planets — planet echoed on the ref and the event, the biome named by `PlanetDef.biome`, node count from `gatherDensity`, only that planet's herb ids placed, the ecosystem intact on `EnemySystem.debugEcology`, `pressure` on the ambient cap, 24 sampled patrols per threat + 24 waves per index 0..7 with no type outside `eco.bugs` and no broken threat / wave gate, no artillery in a group, behemoths only where the weights and `maxBehemoth` allow, guard density from `eco.rogues` and the `eco.boss` rule; the herb weights over 3 seeds, shape ≠ herb id, determinism (same seed + planet → identical biome / node ids / positions / guard placement, a different planet differs), an unknown planet id falling back to the seeded draw and a training staying planet-free (83). Parks the vite HMR + relay sockets. |
| `smoke-planets.mjs` | **Phase 11 행성 선택** (`src/hub`, single-player, no relay): the full-screen terminal (three columns, the 승무원 이름 section gone, `.seed-hint` kept, 닫기 (Esc), `hub:terminalToggled`, the Phase 10 cursor etiquette — `'hub'` blocker + `setCursorMode`, never `exitPointerLock`), the 행성 홀로그램 canvas and its ◀ ▶ / arrow / A-D stepping (**preview only** — `ctx.hub.planet` does not move until 행성 이동), 행성 이동 → `hub:travel {stage:'start'}` → the warp cutscene → `hub:travel {stage:'end'}` + `hub:planetChanged`, the window planet re-tinted to the destination, `PLANET_STORAGE_KEY` persistence across a reload, the terminal screen's `목표 <행성>` line, `setPlanet` refusals (unknown id / travelling / mid-countdown), the launch-slot gate (`목표 행성 미지정 — 터미널에서 지정` prompt + refusal, boarding allowed once a planet is set) and `game:newMission {planet}` / `ctx.missionPlanet` at launch (76). The lobby path — host picks, guest mirrors, non-host refused — is `e2e:mp`'s. |
| `smoke-library.mjs` | 서재 책장 (Phase 9, housing + items + hub): the 14 서적 defs (one per skill, `book_<skill>`, 한국어 『』 names, 1×2 / stackMax 1 / 0.6 kg, rarity spread common 4 / uncommon 5 / rare 3 / epic 2, `book` label · colour · icon, never craftable), books in the tier 2–4 loot tables and on 로그 / 보스 시체 but never tier 1 or a bug corpse; 방 4 → 서재, `furn_bookshelf` craft / `canPlace` (서재 only) / place, `getBooks` → 6 empty slots; `placeBook` (bag first, 창고 fallback, `housing:booksChanged` + `housing:changed {bookPlace}`, 도감 entry) and its refusals (duplicate slot, non-book def, missing stock, slot out of range, wrong uid); `getBookBonus` = `1 + BOOK_XP_PER_BOOK × Σ BOOK_RARITY_MUL`, additive stacking, 1 for an unrelated skill, `BOOK_GAIN_MAX` cap at 6 epic books, folded into `getSkillGainMul` next to the 사격장 ×1.1; `takeBook` (back to the bag, bonus drops, 도감 kept, 공간 없음 refusal), recovering a full shelf hands its books to the 창고 and the same recover / `purposeBlock('empty')` is refused with `책을 먼저 빼세요` when they do not fit; v3 save round-trip + a hand-corrupted `scav.ship` (bad slot / unknown uid / non-book id / duplicate slot / unknown def) sanitised without a throw; the 책장 패널 (blocker, `ui:bookshelfToggled`, 6 slot cards, 보유 서적 picker, 꽂기 button, 14 도감 rows with live multipliers, Esc) and the 함선 탭 도감 through `createShipView` (126). Parks the vite HMR + relay sockets. |
| `smoke-enemy-delta.mjs` | **Phase 9 delta enemy snapshots** (single-player, no relay — drives `EnemySystem.debugSnapshot / debugApplySnapshot / debugSnapshotState` directly): the host encoder (`SnapshotCache` — first snapshot is a keyframe with every field, a still enemy drops out of the next delta, a moving one carries only `p`, an hp change only `hp`, a despawn lands in `gone`, keyframes return every `NET_ENEMY_KEYFRAME_S × NET_ENEMY_SNAPSHOT_HZ` snapshots and `seq` is monotonic), the replica intake (`applyWire` lays a partial wire over the latest sample, an omitted enemy holds still, an unknown id without `ty` is ignored and counted), the keyframe forced by a promotion / `flow rejoined`, the burn credit (`applyStatus(..., attacker)` → `enemy:killed.by`) and enemy fire stopped by a 배리어 (pure `raycastBarrier` + one `damageBarrier`). Registered in `verify.mjs` for `enemies` / `net` (52). |
| `smoke-ghost.mjs` | Player (Phase 7): `restoreState` 0 / 1 / 2 (no hellpod, no `player:died`), `world:ready` skipped while `ctx.rejoinPending`, `isMeleeHeavy`, knockback (ignored downed / dead, lift-off, roll cancel, no `player:launched`), remote avatar THROWING / COOKING / CHARGING / SPRAYING / HEAVY / MELEE_HEAVY / OVERCHARGED poses + held-item mesh (`heldItemId`) + armor plates (`armorId`) via `debugSpawn`, host ghosts via `debugSuspend` (creation, `ghost:damage` → downed → bleed → dead, kb nudge, revive interactable → `ghostq revive`, demotion / promotion rebuild, `debugRejoin` restore wire); **Phase 9**: a ghost made from a downed ref inherits that member's own `downHp` (`debugSpawn({downHp})`, clamped 1..`PLAYER_DOWN_HP`, unknown → the full pool) and **parked ghosts** — `net:missionMembership {inMission:false}` or `net:peerSuspended {suspended:false}` parks the wire for `NET_GHOST_PARK_S` (3600 s since 2026-09-07 — the whole raid; no simulation, no `ghost:damage`, no broadcasts), `debugRejoin` restores from it exactly once, `debugExpireParked` / demotion / `game:abort` drop it (86). Parks the `vite-hmr` socket. |
| `smoke-meta.mjs` | Meta (Phase 5-c): fresh save, rep level → corp shop filter / rarity cap / prices, buy + refusal, sell (bag → credits), contract accept gating, kill progress from a real enemy, death / extraction settlement, squad share, quest h1 deliver → rewards → h2 unlock, reload persistence + corrupt-save sanitising, 기업 네트워크 screen — **2026-09-07**: it is the Tab window's 기업 tab (`.inv-screen.corp-view`, no `.menu.corp-menu`, no `'corp'` blocker), so the checks cover the rail / 기업 패널 + 세로 페이지 탭 / footer-less layout, the locked stock grid keeping its shape with a centred reason, the 5-column 구매·판매 trays and the shared `--ct-cell` / `--inv-cell` 40 px, footprint-spanning stock cells, 귀중품 전부 담기 under the 판매 tray, and Escape closing the window; Phase 7: `canFit` → 공간 없음 before the click, server credits through a fake `ctx.net.profile` (optimistic debit → tx → `meta:purchase`, refusal revert, `profile.set('meta')`, `net:profileLoaded` replace / migrate), settlement `outcome`, training settles nothing + goal counters off (126). The three single-player scripts park the relay socket (`/ws?`) as well as vite's HMR socket so a relay on 8787 cannot hand the page a real profile mid-run. |
| `smoke-training.mjs` | 시뮬레이션 훈련장 (Phase 7 §9, world + hub): a real `furn_sim_hub` placed through `ctx.housing` in a 사격장 room (holo pedestal, spinning rings, no light, `hub_furn_<uid>` prompt), E → `game:newMission {mode:'training'}` → arena world (mode / size 64 / no extraction · crates · nests · gather · enemy spawns, 12 targets + exit console obstacles, space-mode look, `ui:objective` counter, south spawn), arena queries (height 0, bounds, collision clamp, raycast floor / wall / ceiling / target destructible), the real gun knocks target 0 down (`weapon:hit` enemyId null → `onDamage`), hinge animation, hash removal while down, pop-up after `TRAINING_TARGET_RESPAWN_S`, exit console → one `training:exitRequested` per press (+ the return to the ship: by game/ when implemented, else the script aborts), faked lobby (`NetSystem._lobby`, no relay): shared-ship terminal 시뮬레이션 훈련장 section 시작 / `합류 (n명 훈련 중)` / 임무 진행 중, crew row 훈련장, pod prompt + refusal + status line while a training runs; **Phase 9 target modes**: the 모드 콘솔 cycles 고정 → 이동 → 타임 코스 (`training:modeChanged`, prompt text, `ctx.world.training.mode`), a 이동 표적 sweeps inside `TRAINING_MOVING_SPAN` of its lane centre and is still hit by a raycast at its new x, the 타임 코스 starts from the console (`startCourse`, `remaining` ticking, `training:scored` per knock-down, `training:courseFinished` with `best` saved to `TRAINING_BEST_STORAGE_KEY`), and the 무기 거치대 opens the 무한 상자 on its 주무기 tab (`ui:catalogToggled`) with the loadout restored on exit (109). |
| `smoke-raidflow.mjs` | Phase 7 game flow (solo + synthetic net events): solo death → `game:raidFailed` + `game:over` → 레이드 실패 screen → auto return to the ship (`RAID_FAILED_AUTO_RETURN_S`), `game:respawn` refused; 훈련장 enter (`missionMode` / `stats.mode` / no threat) → death = instant respawn → exit console → abort + `applyRaidState(snapshot)` + personal ship, no XP / settlement; rejoin (`net:raidLoaded` blob → stats / missionTime, `net:ghostRestore` alive → playing, dead → 30 s countdown, no restore → hellpod fallback), `saveRaid` never called solo; **Phase 9** `player:giveUpProgress` during the downed give-up hold (43). Parks the `vite-hmr` socket. |
| `shots-uiux.mjs` | **Screenshot helper, not a smoke** (no assertions, no exit code, not registered in `verify.mjs`): drives the hub Tab window through the 2026-09-07 UI/UX pass — 함선 인벤토리, the 3-column 캐릭터 tab, the 전술 임플란트 picker, the 기업 거래 / 계약 / 퀘스트 desk and the 제작 column in both the ship and a raid — and writes `scripts/shots/uiux-*.png`. Use it to eyeball a layout change; `node scripts/shots-uiux.mjs [url]`. |
| `logs/`, `shots/` | Runner output and screenshots (git-ignored). |

Every smoke script takes the vite URL as its first argument (default `http://localhost:5273/`), launches its own headless
Chrome on the **real GPU** (`GL_ARGS`: ANGLE D3D11, 10–50 s per script; `SMOKE_GL=swiftshader` = CPU rasterizer, ~10× slower),
stubs `requestPointerLock` (a real lock would trap the OS cursor in the hidden window — Windows ClipCursor) and fakes
`pointerLockElement`, waits on **simulation time** (`ctx.time`) and dispatches key taps as keydown+keyup in one frame on
`document.body`. `waitFor` defaults to 60 s so four scripts can share the machine. Checks build on each other, so the unit of
re-run is the whole script.
