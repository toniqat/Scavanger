# SCAVANGER — 콘텐츠 카탈로그 (포트폴리오 서술 단위)

> [PORTFOLIO.md](PORTFOLIO.md)의 부속 문서. **"어필하기 좋은 단위"로 쪼갠 구현 콘텐츠 목록**이다.
> 각 단위는 ① 한 줄 정의 ② **기획 의도**(왜 넣었나 / 무슨 문제를 푸나) ③ 구현 스펙 ④ 포트폴리오 서술 각도 ⑤ 근거 경로로 구성된다.
>
> **등급 표기**
> **★★★** 단독 슬라이드·챕터 가치 (설계 판단이 드러남) · **★★** 2~3개 묶어 한 장 · **★** 목록·표에 이름만
>
> 총 **52개 단위 / 8개 군**. 앞에 있는 군일수록 "이 게임이 왜 이렇게 생겼는가"를 설명한다.

---

## 목차

| 군 | 주제 | 단위 수 | ★★★ 개수 |
|---|---|---|---|
| A | 코어 루프 · 익스트랙션 구조 | 6 | 4 |
| B | 캐릭터 성장 | 6 | 3 |
| C | 전투 · 전술 | 7 | 3 |
| D | 레이드 월드 콘텐츠 | 9 | 4 |
| E | 아이템 · 인벤토리 · 경제 | 8 | 4 |
| F | 함선 생활 콘텐츠 | 9 | 4 |
| G | 메타 · 기업 · 서사 | 4 | 2 |
| H | 협동 · 온보딩 · 접근성 | 3 | 2 |

---

# A. 코어 루프 · 익스트랙션 구조

> **이 군의 서술 목표**: "RPG 축으로 판 익스트랙션 슈터"라는 기획 의도가 *구조로* 구현되었음을 증명한다.
> 포트폴리오에서 가장 앞에 오고, 가장 많은 지면을 써야 하는 군이다.

### A-1. 함선 목표 → 행성 선택 루프 ★★★

- **정의**: 함선 시설에서 하고 싶은 일이 다음 레이드의 목적지를 결정하는 구조.
- **기획 의도**: 익스트랙션 슈터가 흔히 답하지 못하는 질문이 **"왜 하필 저 행성인가"**다. 보통은 난이도와 드랍 등급뿐이라
  결국 "제일 비싼 데"로 수렴한다. 나는 이 질문의 답을 **레이드 밖(함선)에서 만들도록** 뒤집었다.
  행성마다 **나는 흙·씨앗·표본 계열·광맥 등급이 다르므로**, "온실에서 서리열매를 기르고 싶다"는 생활 목표가
  곧 "보레아스 IX로 가야 한다"는 전술 결정이 된다. 난이도와 무관한 **두 번째 목적지 선택 축**을 만든 것이다.
- **구현**: 행성 5종에 `soils` / `seeds` / `samples` / `mineralNodes` / `herbs` / `env` / `hazards` 열이 각각 다르게 배정.
  아켈론 II(부엽토·화산재토 / 잿빛 곡물) · 보레아스 IX(동토 이탄 / 서리열매) · 베르단트 III(포자 부식토 / 포자 갓) ·
  피로스 VII(광물토·열 환경 / 광맥 7) · 카민 I(광물토·독 환경 / 포그 없음).
- **서술 각도**: **다이어그램 1장 + 행성 대조표 1장.** "난이도 순서 = 전리품 등급 순서"라는 1차 축 위에
  "생활 콘텐츠 수요"라는 2차 축을 교차시켰다고 설명한다.
- **근거**: `data/planets.csv` (컬럼 주석에 의도 서술), `src/world/README.md`

### A-2. 손실 설계 — PvE에서 긴장을 만드는 법 ★★★

- **정의**: 완전 사망 시 전 장비 상실, 자동 부활 없음. 단 회수 경로를 남긴다.
- **기획 의도**: PvP를 빼면 "다른 플레이어에게 털린다"는 긴장 장치가 사라진다. 그 자리를 **벌칙 강화**로 메우면
  타르코프 아류가 되고, **벌칙 완화**로 메우면 익스트랙션일 이유가 없어진다. 그래서 벌칙의 *총량*이 아니라
  **성질**을 바꿨다 — 잃되, **잃은 것을 되찾으러 가는 플레이가 새로 생기도록** 설계했다.
- **구현**:
  - 사망 → 장비·가방·퀵슬롯 전부 시체로 (`InventoryRef.stripForCorpse`)
  - **분대 레이드에서만** 임플란트가 '망가진 짝'으로 시체에 남음 → 세레스 바이오에서 수리 가능 / 솔로는 소실
  - 부활은 **구조선 투하(분대 공용 5회, 쿨 30초)로 맨손 강하**만 → 부활 직후 목표가 "내 시체 회수"로 자동 생성
  - 빈 시체는 `CORPSE_EMPTY_REMOVE_DELAY_S` 후 가라앉되, **누군가 루팅 창을 열어 둔 동안은 남는다**
  - 레이드 실패 조건: 인간 전원 **그리고** 안드로이드 전원이 쓰러져야 함
- **서술 각도**: "하드코어를 낮췄다"가 아니라 **"벌칙 이후의 플레이를 설계했다"**. 실패 처리를
  콘텐츠로 전환한 사례로 쓰면 콘텐츠 기획 역량으로 읽힌다.
- **근거**: `CLAUDE.md` §4.6, `src/game/parts/Death.ts`, `docs/DECISIONS.md 「2026-09-09 — 사망/시체」`

### A-3. 탈출 시퀀스 ★★★

- **정의**: 콘솔 → 20초 호출 → 탈출선 착륙 → 탑승 → 10초 취소 불가 유예 → 탄 사람만 이륙.
- **기획 의도**: 익스트랙션의 클라이맥스가 "버튼 누르고 기다리기"로 끝나면 안 된다. 세 가지 결정을 강제하도록 설계했다.
  ① **호출 = 위치 노출** (조명탄 + 적 인지), ② **탈출선 외피가 실제 콜리전**이라 적이 못 들어오는 대신
  플레이어도 문으로만 들어간다 → 마지막 순간의 공간 싸움, ③ **취소 불가 유예 10초** —
  "한 명 더 기다릴까"를 *되돌릴 수 없게* 만들어 분대 내 결정을 실제 갈등으로 만든다.
  남겨진 사람은 죽지 않고 **다시 호출할 수 있다** (실패가 아니라 재시도).
- **구현**: 패드 여러 곳 · 콘솔 상호작용 · 착륙 시 선체 콜리전 · 적 진입 차단 쿼리 · 이륙 시네마틱(HUD 전체 은폐) ·
  탑승자만 정산 · 남은 인원은 리셋 후 재호출. 호스트 권한.
- **서술 각도**: **"취소 불가 유예"라는 단일 규칙이 만들어내는 분대 드라마**를 설명하면 시스템→감정 설계의 예시가 된다.
- **근거**: `src/extraction/README.md`, `CLAUDE.md` §4.6

### A-4. 레이드 재개 (이어하기 / 레이드 포기) ★★★

- **정의**: 접속이 끊겨도 진행 중이던 레이드로 돌아갈 수 있고, 돌아가지 않기로 선택할 수도 있다.
- **기획 의도**: 익스트랙션 장르의 가장 큰 이탈 원인은 **"한 판이 통째로 날아가는 사고"**다.
  브라우저 게임이면 더 심하다(탭 닫힘, 새로고침). 그래서 레이드를 **세션으로 저장**했다.
  중요한 건 **죽음을 반드시 기록한다**는 점이다 — 죽은 뒤 껐다 켜서 장비를 되살리는 악용을 막기 위해,
  시체 생성 직후 강제 저장하고 새 저장 경로는 전부 "이미 죽었는가"를 먼저 검사한다.
  그리고 **돌아가지 않을 권리**도 줬다: 타이틀에서 `레이드 포기`를 고르면 정산되고 분대에서 '표류' 처리되어
  구조 대상에서도 빠진다 — 남은 분대원이 오지 않을 사람을 기다리지 않게 하는 배려다.
- **구현**: 분대는 릴레이 blob, 솔로·튜토리얼은 localStorage. 5분 재접속 유예. 타이틀 `이어하기` / `레이드 포기`.
  재접속 시 실드 같은 별도 풀까지 복원(누락 시 최대치로 간주하지 않도록 명시적 전송).
- **서술 각도**: "사고를 콘텐츠 손실로 만들지 않는다"는 라이브 서비스 감각. 대형사에서 가장 잘 읽히는 종류의 판단.
- **근거**: `src/game/parts/Session.ts`, `parts/Resume.ts`, `CLAUDE.md` §4.3

### A-5. 발사 포드 · 준비 시스템 ★★

- **정의**: 함선 발사 슬롯에 앉아 홀드로 준비 완료 → 분대장이 발사.
- **기획 의도**: "로비 대기 → 버튼" 대신 **공간으로 준비 상태를 표현**한다. 앉는 것과 준비는 별개다
  (탑승 ≠ 준비) — 자리에 앉아 장비를 마지막으로 만지는 시간을 보장하기 위해서다.
  각자의 준비 상태가 슬롯 위에 보이므로, 누가 아직인지 말로 물을 필요가 없다(§H-1과 같은 철학).
- **구현**: 슬롯 준비 셀 · 안드로이드는 봇 슬롯으로 자동 준비 · 진입 페이드 후 로딩 게이트(§H-2).
- **근거**: `src/hub/README.md`, `docs/DECISIONS.md 「2026-09-14 — 발사 슬롯」`

### A-6. 레이드 흐름 상태 기계 ★

- **정의**: 함선 → 도킹 → 발사 → 레이드 → 탈출/사망 → 결과 → 함선 복귀의 위상 관리.
- **기획 의도**: 각 위상마다 **ESC가 무엇을 하는지, 무엇을 저장하는지, HUD가 무엇을 숨기는지**가 달라야 한다.
  이걸 화면마다 따로 짜면 반드시 어긋나므로 **정책을 한 파일에만** 두었다.
- **구현**: `GameFlowSystem`. 결과 화면 → 함선 복귀는 `ui:shipReturn`(페이드 → 로딩 게이지 → 페이드 인) 전용 경로.
- **근거**: `docs/ARCHITECTURE.md` §2~3, `src/game/parts/Phases.ts`

---

# B. 캐릭터 성장

> **서술 목표**: "RPG를 깊게 판다"는 말을 **성장 소스 4겹 + 단일 합류점**이라는 구조로 증명한다.

### B-1. 능력치 5종 ★★

- **정의**: 근력 · 지구력 · 인지력 · 지능 · 재주.
- **기획 의도**: 전투 능력치를 일부러 **넣지 않았다**. 5종 전부가 *생활/탐색*에 걸리고(적재량·스태미나·감지 범위·
  숙련 상승률·상호작용 속도), 전투력은 **무기와 숙련도**가 담당하도록 분리했다.
  "스탯을 올려서 총이 세지는" 구조를 피해, **스탯 = 레이드를 얼마나 오래·많이 돌 수 있는가**로 의미를 몰아준 것이다.
  익스트랙션에서 실제로 승패를 가르는 건 화력이 아니라 **얼마나 들고 나올 수 있는가**이기 때문.
- **구현**: `data/stats.csv`. 각 행에 `derived` 열로 **캐릭터 시트의 어느 줄을 바꾸는지** 명시 →
  시트에서 능력치에 마우스를 올리면 영향받는 줄이 강조된다.
- **서술 각도**: "스탯 5종이 전부 비전투다"라는 선택 자체가 기획 의도의 선언이다.
- **근거**: `data/stats.csv`

### B-2. 숙련도 16종 — 행동으로만 오른다 ★★★

- **정의**: 사격 5(AR/SMG/SR/DMR/SG) + 생활 11(운반·감정·인내·원예·제작·의학·암호학·임플란트·장비관리·요리·연구·채광).
- **기획 의도**: 포인트로 찍는 성장은 "빌드 선택"이고, **행동으로 오르는 성장은 "플레이 기록"**이다.
  익스트랙션에서 후자가 더 맞다고 봤다 — 상자를 많이 뒤진 사람은 감정이 높고, 무거운 짐을 진 사람은 운반이 높다.
  **내가 어떻게 플레이했는지가 캐릭터에 새겨지는** 구조다.
  각 숙련은 어떤 능력치에 XP를 나눠 주는지(`stats` 열)까지 정의되어, 행동 → 숙련 → 능력치의 3단 파급이 생긴다.
- **구현**: `data/skills.csv`. 사격 숙련은 무기 계열별로 분리(맞혀야 오름) · 생활 숙련은 각 행동에 연결.
  **설계 판단**: 제작 숙련은 제작을 *막지도, 빠르게 하지도* 않는다 — 하는 일은 **재료 환급 확률** 하나뿐(§E-5).
- **서술 각도**: "왜 제작 숙련이 제작을 안 막는가"를 설명하면 **성장의 벌칙화를 피하는 판단**으로 읽힌다.
  (숙련이 낮으면 못 만든다 = 초반 플레이어에게 콘텐츠를 잠그는 것. 대신 잘하면 이득으로 설계.)
- **근거**: `data/skills.csv`, `docs/DECISIONS.md 「2026-09-16 — 제작과 숙련」`

### B-3. 파생 능력치 단일 합류점 (`DerivedStats`) ★★★

- **정의**: 능력치 · 숙련도 · 단련 · 식사 버프 · 서재 효과 · 임플란트가 **전부 하나의 구조체로 합류**한다.
- **기획 의도**: 성장 소스를 늘릴 때마다 소비처(무기·플레이어·인벤토리)를 고쳐야 하면 **콘텐츠 확장 비용이 선형으로 증가**한다.
  그래서 "무엇이 이 수치를 올렸는가"를 소비처가 몰라도 되게 만들었다. 새 버프 종류를 추가해도 소비처 코드는 그대로다.
  → **콘텐츠 추가 비용을 상수로 만든 설계.**
- **구현**: `computeDerived(profile, backpack, implants)` + `applyMealBuff` + `applyLibraryDerived`.
  소비처 규약: `ctx.progression?.derived`를 **중립 기본값**(배수 1, 가산 0)으로 읽는다.
- **서술 각도**: 시스템 기획 역량을 보여주는 최고의 단위. "확장성"을 말로 하지 않고 구조로 보여줄 수 있다.
- **근거**: `src/progression/derive.ts`, `src/progression/README.md`

### B-4. 전술 임플란트 5종 ★★

- **정의**: 갈고리 · 대시 · 배리어 · 오버차지 · 정찰. Q 하나로 구동, 함선에서만 교체.
- **기획 의도**: 헬다이버스식 "능력 슬롯"을 익스트랙션에 맞게 **하나로 제한**했다.
  여러 개를 돌려 쓰면 전투가 능력 사이클이 되지만, **하나뿐이면 그 능력이 곧 캐릭터의 성격**이 된다.
  또 "함선에서만 교체"라 **출발 전 결정**이 되고, 이것이 행성 선택·분대 역할과 맞물린다.
  배리어를 들면 총을 못 쓰는 등, 능력마다 **포기해야 하는 것**을 붙여 무비용 능력이 없게 했다.
- **구현**: 즉발형(갈고리·대시·배리어 토글) / 홀드형(정찰 펄스·오버차지 채널). 대시만 3충전.
  배리어는 실제 물리 충돌체 + 실드 배쉬. 정찰은 분대 공유. 모든 판정은 시전자 클라이언트, 타인에겐 `buff`로만 전달.
- **근거**: `src/implants/README.md`, `src/shared/implants.ts`

### B-5. 임플란트 아이템 + 망가진 짝 + 수리 데스크 ★★

- **정의**: 장착칸을 소모하는 수동형 강화 아이템. 등급 5단계(일반~전설) · 전설 퍽 3종.
- **기획 의도**: §A-2의 손실 설계와 짝을 이룬다. 임플란트가 **사망 시 '망가진 짝'으로 시체에 남게** 한 이유는,
  "잃었다"와 "영영 사라졌다" 사이에 **한 칸을 더 만들기 위해서**다. 망가진 것을 주워 오면
  세레스 바이오에서 재료를 들여 되살릴 수 있다 → 실패한 레이드에도 회수 가치가 생긴다.
  솔로에서는 그냥 소실시켜 **분대 플레이의 실질 이점**을 하나 만들었다.
- **구현**: 등급별 가격·수리 재료표(`implants_repair.csv`) · 등급별 장착칸 수 · 전설 퍽 3종
  (자동 부활 3칸 / 빠른 치료 2칸 / 처치 시 스태미나 3칸 — 각각 능력치 +1 덤).
- **근거**: `data/implants_perks.csv`, `data/implants_repair.csv`, `src/meta/parts/ImplantDesk.ts`

### B-6. 캐릭터 슬롯 3 · 생성 · 시트 ★

- **정의**: 슬롯 3개 독립 세이브, 생성 시 3D 프리뷰 + 능력치 배분 + 시작 임플란트 선택.
- **기획 의도**: 익스트랙션은 **빌드를 바꾸려면 캐릭터를 새로 만드는** 장르 관습이 있다. 슬롯을 나눠 실험을 허용하되,
  저장 키를 슬롯 단위로 완전히 분리해 섞이지 않게 했다. 스탯 배분은 **확정 전까지 보류 상태**로 두고
  나가려 하면 경고한다 — 되돌릴 수 없는 결정에는 반드시 확인을 붙인다는 전역 UX 규칙(§E-8)의 적용례.
- **근거**: `src/shared/saveSlot.ts`, `src/progression/README.md`

---

# C. 전투 · 전술

> **서술 목표**: 슈팅이 장르 기준을 만족한다는 것 + 전투 콘텐츠가 **선택지**로 설계되었다는 것.

### C-1. 무기 5계열 × 등급 × 희귀도 + 내구도 ★★

- **정의**: AR · SMG · SR · DMR · SG. 등급(I~V)과 희귀도가 별개 축. 주무기 2슬롯.
- **기획 의도**: 총기를 **아이템으로 취급**하는 것이 이 장르의 핵심이다. 그래서
  ① **등급**(기본 성능) ② **희귀도**(옵션·가치) ③ **내구도**(소모) ④ **부착물 소켓**을 분리해,
  같은 계열 총기도 개체마다 다르게 만들었다. "AR을 하나 더 주웠다"가 의미를 갖도록.
  등급이 오르면 **조작성(반동·장전)도 함께** 오르게 해(`WEAPON_GRADE_HANDLING_MUL`)
  상위 등급이 수치뿐 아니라 **손맛으로 체감**되게 했다.
- **구현**: `data/weapons.csv`(계열 5) + `attachments.csv`(부착물 15) + `sockets` 열로 계열별 허용 소켓 정의.
  맞지 않는 부착물은 장착돼도 효과 없음. 내구도 5구간이 수리비·분해 수율과 연동(§E-4).
- **근거**: `data/weapons.csv`, `src/items/WeaponStats.ts`

### C-2. 유니크 무기 6종 (클래스 규칙 밖) ★★★

- **정의**: 인페르노(화염방사) · 테슬라 코일(전격) · 카게(수리검) · 롱혼(활) · 해머헤드(바주카) · 사이클론(미니건).
- **기획 의도**: 일반 총기를 수치로 촘촘하게 짜 놓으면 **"최종 목표"가 그저 더 큰 숫자**가 된다.
  그래서 최상위 보상은 수치가 아니라 **작동 방식 자체가 다른 무기**로 설계했다 —
  각각 전용 탄약(연료·셀·수리검·화살·로켓·벨트)을 쓰고, 클래스 밸런스 규칙의 **예외**로 명시했다.
  전용 탄약은 동시에 **제약**이다: 강력하지만 보급이 어려워 "언제 꺼낼 것인가"라는 판단이 붙는다.
  획득 경로도 좁혔다 — **신화 광물**(threat 3 행성 깊은 광맥에서만 극히 드물게)이 유일한 재료.
- **구현**: `data/weapons_unique.csv` 6행 + 전용 탄약 타입 + `def.unique` 플래그(클래스 규칙 무시).
  신화 등급은 **총기 계열에만** 남긴다는 결정으로 희소성 축을 하나로 정리(2026-09-17).
- **서술 각도**: **"최종 보상을 숫자가 아니라 동사(動詞)로 줬다"** — 가장 기획자다운 한 줄이 나오는 단위.
- **근거**: `data/weapons_unique.csv`, `docs/DECISIONS.md 「2026-09-16 — 신화 등급」`

### C-3. 방탄복 = 실드 풀 ★★

- **정의**: 방어구는 피해 감소가 아니라 **재충전 가능한 실드 수치**(등급별 20~100).
- **기획 의도**: 피해 감소(%)는 플레이어가 **체감할 수 없는** 수치다. 20% 감소는 화면에 아무것도 안 보인다.
  그래서 방어를 **눈에 보이는 별도 게이지**로 바꿨다. 함선에서 자동 충전, 레이드 중엔 **충전기 아이템으로만** 회복 →
  "방어구를 얼마나 아껴 쓸 것인가"가 자원 관리 판단이 된다.
  **낙하 피해는 실드를 무시**하게 해서, 실드가 만능 방패가 되지 않도록 예외를 한 종류만 뒀다.
- **구현**: `ARMOR_SHIELD_BY_TIER`, 충전기 3종, `damageReduction`은 0으로 고정, `bypassShield` 플래그.
- **근거**: `data/armor.csv`, `CLAUDE.md` §4.7

### C-4. 가젯 13종 (호스트 권한 배치물) ★★

- **정의**: 지뢰 · 원격 지뢰 · 자동 포탑 · 돔 실드 · 바리케이드 · 점프대 · 은폐 장막 · 유인 수류탄 · 연막탄 ·
  제세동기 · 지상 드론 · 공중 드론 · 진동 장치.
- **기획 의도**: 총 이외의 **공간을 바꾸는 수단**을 소모품으로 줬다. 소모품이기 때문에
  "지금 쓸까, 아껴서 들고 나갈까"가 매번 판단이 된다(= 전리품과 경쟁하는 인벤토리 무게).
  **배치 미리보기와 실제 배치가 같은 함수로 판정**되게 해서(§E-7), 초록인데 안 놓이는 상황을 구조적으로 제거했다.
- **구현**: 배치물은 호스트 권한 동기화, 드론은 소유자 권한. 원격 지뢰는 같은 대상에 **중첩 감쇠**
  (`max + (합−max) × 계수`)로 도배 전술 차단.
- **근거**: `src/gadgets/README.md`, `data/items.csv`(gad_*)

### C-5. 함선 지원 4종 (공유 쿨다운) ★★★

- **정의**: 궤도 폭격 · 보급품 투하 · 트라이포드 투하 · 구조선 투하. G 휠 4방위 고정.
- **기획 의도**: 헬다이버스식 스트라타젬을 그대로 가져오면 **능력 쿨다운 관리 게임**이 된다.
  그래서 **네 종류가 하나의 쿨다운을 공유**하게 했다 — 어느 하나를 쓰면 나머지 셋도 잠긴다.
  이 규칙 하나로 호출이 **"무엇을 쓸까"가 아니라 "지금 이걸 쓸 값어치가 있나"**라는 질문이 된다.
  쿨다운 길이를 용도별로 다르게 둔 것도 같은 이유다 — 구조선 30초(사람을 살리는 건 자주 허용),
  보급/구조물 90초, 궤도 폭격 120초(피아 무구분이라 가장 비싸다).
  **항공 폭탄은 정의만 남기고 휠에서 내렸다** — 4칸이라는 제약을 지키기 위해 만든 것을 뺀 결정.
- **구현**: `data/stratagems.csv`. 탑뷰 조준(궤도) / 지상 조준(투하). 분대 호출은 호스트가 중계하고,
  거절되면 **쿨다운 전액 환불**(호스트가 보낸 내 `callId`에만 반응).
- **서술 각도**: **"제약을 늘려 선택을 만든 사례"**. 4칸 고정 + 공유 쿨다운 + 기능 하나 삭제까지 묶어 서술.
- **근거**: `data/stratagems.csv`, `src/stratagems/README.md`

### C-6. 사격 감각 규칙 세트 ★

- **정의**: 크로스헤어 선 기준 명중 판정 + 3 m 총구 차폐 + 발사체 스윕 + 거리 감쇠 + 조준 흔들림.
- **기획 의도**: 3인칭 슈터의 고질적 불만은 **"조준점에 맞았는데 벽에 맞았다"**(총구가 벽 뒤)와
  그 반대(벽 너머로 쏴진다)다. 그래서 **명중은 크로스헤어 선에서 판정하되, 총구 3 m 이내만 차폐**로 타협했다.
  조준 흔들림은 조준 중에만 8자 궤적으로 넣어 **정밀 사격에 시간 비용**을 붙였다.
- **구현**: `weapons/parts/AimLine`, `ProjectilePool`(관통 방지 스윕), `data/aim_sway.csv`(자세/계열/이동별).
- **근거**: `CLAUDE.md` §4.6, `src/weapons/README.md`

### C-7. 근접 · 투척 · 퀵유즈 휠 ★

- **정의**: 근접 공격, 수류탄(궤적 미리보기), 소모품 퀵슬롯 휠.
- **기획 의도**: 투척 거리를 **근력에 연동**해 능력치가 전투에서도 체감되게 하되(§B-1의 예외),
  수치가 아니라 **미터 단위 궤적선**으로 보여줘 학습 가능하게 했다.
- **근거**: `src/weapons/README.md`, `data/constants.csv`

---

# D. 레이드 월드 콘텐츠

> **서술 목표**: 절차 생성 맵에 **손으로 설계한 콘텐츠 포인트**를 어떻게 심었는가.

### D-1. 버려진 구조물 3종 + 잠금 구조 ★★★

- **정의**: 전진기지 · 버려진 연구실 · 불시착한 함선. 진입 가능, 지하실 / 2층 잠긴 방 / 옥상.
- **기획 의도**: 절차 생성 맵의 약점은 **"어디를 가도 똑같다"**는 것이다. 그래서 맵에
  **성격이 다른 목적지**를 심고, 각각에 **다른 보상 티어와 다른 진입 조건**을 붙였다.
  - 전진기지 = **지하실**(65% 확률, 열쇠 필요) — 무장·탄약 중심
  - 연구실 = **2층 잠긴 방**(키카드 필요) — 감정 가치 높은 3~4티어
  - 불시착 함선 = 잠금 없음, 대신 **귀중품·취미품 4티어가 유일하게 흔한 자리**
  열쇠를 **소모형 만능 키**로 만든 게 핵심 판단이다: 특정 건물 전용이면 "열쇠는 있는데 그 건물이 안 나왔다"가
  되어 버린다. 같은 종류면 어느 건물이든 열리되 **열 때 1개 소모** → 열쇠 자체가 자원이 된다.
  열쇠는 지상층 컨테이너에서 **5% 부가 굴림**으로만 나오므로, "지하실을 봤는데 열쇠가 없다"는 상황이
  다음 레이드의 동기가 된다.
- **구현**: `data/structures.csv` — 개수·크기·컨테이너 수·티어 가중치·지하실 확률·2층 확률·열쇠 id·열쇠 확률까지 전부 데이터.
  층/계단/옥상/사다리/창문(깨져도 사람은 못 지나감) 절차 생성.
- **서술 각도**: **"절차 생성 + 손설계 목적지"의 결합**. 열쇠 설계 판단은 단독으로 한 문단 값어치가 있다.
- **근거**: `data/structures.csv`(주석에 설계 이력 포함), `src/world/README.md`

### D-2. 환경 재해 4종 ★★★

- **정의**: 모래 폭풍 · 눈보라 · 폭풍의 눈 · 독성 포자. 레이드마다 시드로 하나 추첨.
- **기획 의도**: 레이드에 **시간 압박**을 주되, 타이머 UI로 주고 싶지 않았다.
  "몇 분 남았습니다"는 게임이 말하는 것이고, **다가오는 전선(前線)은 플레이어가 보는 것**이다.
  그래서 재해를 **공간적으로** 만들었다 — 경계 벽이 지평선에서 다가오고, 안으로 들어가면 시야가 좁아진다.
  폭풍의 눈만 시야 배수를 24로(약 15 m) 극단화해서, **"안에서는 앞이 안 보여야 눈(eye)이 눈에 띈다"**는
  연출 의도를 수치로 못 박았다.
  **통신 0** — 재해는 시드와 미션 시각만으로 계산되는 함수라 네트워크 전송이 없다. 4인이 같은 시드를 보면 같은 폭풍을 본다.
- **구현**: `data/hazards.csv`(그림 수치) + `constants.csv`(규칙 수치) 분리. 행성별 재해 후보 배정.
  진행도에 따라 포그·입자 램프. **개선 이력이 데이터 주석에 남아 있다**: "멀리서 다가오는 전선이 안 보여
  '모래 폭풍을 본 적이 없다'는 보고" → 벽 높이 72 m → 110 m, 포그 영향 제거.
- **서술 각도**: **플레이 피드백 → 수치 개선의 기록이 데이터 파일에 남아 있는 사례.** 이건 그대로 보여주면 된다.
- **근거**: `data/hazards.csv`

### D-3. 전장의 안개 ★★

- **정의**: 분대 합산 시야로 맵이 열린다. 발견하지 않은 것은 지도·마커·나침반 어디에도 없다.
- **기획 의도**: 미니맵이 다 보이면 탐색이 사라진다. 그렇다고 지도를 없애면 4인 협동이 불가능하다.
  그래서 **"본 것만 공유된다"**로 절충했다 — 지도는 곧 **분대가 함께 알아낸 것의 기록**이 된다.
  늦게 합류한 사람에게도 동기화해서, 재접속한 사람이 혼자 깜깜한 상황을 막았다.
- **구현**: 로컬에서 스냅샷으로 칠함 · 늦은 합류는 `fogq sync` · `fog.isDiscovered` 게이트.
  구조물 옥상의 **맵 스캐너**가 넓은 범위를 한 번에 여는 보상 장치.
- **근거**: `src/world/README.md`, `CLAUDE.md` §4.4

### D-4. 선로 · 전차 (움직이는 발판) ★★

- **정의**: 맵을 가로지르는 선로, 콘솔로 시동 거는 전차, 그 위에서 싸울 수 있다.
- **기획 의도**: 이동 수단을 **"빠르게 가는 것"이 아니라 "움직이는 전장"**으로 설계했다.
  전차 위에서 사격·탑승·하차가 가능하고, 적도 탈 수 있다. 플랫폼에는 컨테이너가 있어
  **선로 자체가 루팅 경로**가 된다. 정보상에서 "궤도 운행"을 사면 선로가 없는 레이드에도 선로가 생긴다(§G-3).
- **구현**: 이동 플랫폼 위 좌표를 매 프레임 차량 로컬로 재계산(`shared/ride.ts`).
  레이아웃 순서에서 **선로가 1순위**로 깔리고 나머지가 피해 간다.
- **근거**: `src/world/README.md`, `CLAUDE.md` §4.4

### D-5. 적 22종 · 팩션 4 · 행성별 팩션 배정 ★★★

- **정의**: 벌레 계열(잡졸·사냥꾼·전사·분사체·돌격체·포격체·독성체·베헤모스·땅굴벌레) +
  인간형(안드로이드 · 로그 · 로그 분대장 · 레이더 저격/해머/헤비/스캔드론) + 튜토리얼 전용 4.
- **기획 의도**: 적을 **두 축**으로 나눴다. 벌레는 *압박*(수가 많고 달려든다), 인간형은 *전술*(엄폐하고 사격선을 잡는다).
  그리고 **행성 위협도가 곧 인간형 팩션**을 결정하게 했다 —
  threat 1은 안드로이드(약함), 2는 로그/레이더, 3은 레이더만. 즉 **행성을 고르는 것이 적 성격을 고르는 것**이다.
  인간형끼리도 **서로 교전**하고, 벌레와 인간형도 싸운다 → 플레이어가 개입 타이밍을 고를 수 있다.
  **총구 기준 사선 판정**(벽에 막히면 발사 자체를 안 함)으로, 적이 벽을 쏘는 어이없는 장면을 제거했다.
- **구현**: `data/enemies.csv`(hp/속도/반경) + `enemy_abilities.csv`(256행) + 행성별 팩션 표.
  거점 점거 · 레이더 강하 · 시체 루팅(적 상태가 시체 전리품에 반영).
- **서술 각도**: "적 종류 수"가 아니라 **"적 배치가 행성 선택과 연결되는 구조"**를 말해야 한다.
- **근거**: `data/enemies.csv`, `data/planets.csv`, `src/enemies/README.md`

### D-6. 네임드 로그 (레이드당 최대 1명) ★★

- **정의**: 위협도에 따라 확률적으로 등장하는 지정 적. 확정 드롭 보유.
- **기획 의도**: 절차 생성 맵에 **"이번 판은 특별하다"**는 사건을 넣되, 만나면 피할 수 없게 하지는 않았다.
  **레이드당 최대 1명**으로 제한해 조우가 희석되지 않게 했고, 정보상에서 **현상 수배(2,600 C)**를 사면
  지정한 네임드가 반드시 나오게 했다 — 즉 **운에 맡길 수도, 돈으로 살 수도 있는 사건**이다.
- **구현**: `NAMED_ROGUE_CHANCE_BY_THREAT`, 타입별 AI/모델 파일 분리, `enemies/named/Director.ts`.
- **근거**: `src/enemies/README.md`, `data/intel_options.csv`

### D-7. 땅굴벌레 + 진동 장치 (소환형 이벤트) ★★

- **정의**: 지하에서 솟구치는 거대 벌레. 진동 장치를 설치해 **불러낼 수도 있다**.
- **기획 의도**: 보스를 "맵에 놓여 있는 것"이 아니라 **"플레이어가 부르는 것"**으로 만들었다.
  위험을 감수할지 말지를 플레이어가 정하게 하는 것이 익스트랙션의 정신에 맞다.
  진동 장치의 획득 경로를 극단적으로 좁힌 게 핵심이다 — **아켈론 II(난이도 1)의 전진기지 지하실에서만 5%**.
  즉 "제일 쉬운 행성에서 열쇠를 구해 지하실을 털어야 나오는 물건으로, 제일 위험한 이벤트를 부른다"는
  **교차 동선**을 만들었다.
- **구현**: 5번 타격 → 소환. 회수 불가, 분화 반경 안에서만 파괴됨. 위협 1 행성에는 약화 개체.
- **근거**: `data/structures.csv`(basementBonus 절), `docs/DECISIONS.md 「2026-09-15 — 땅굴벌레」`

### D-8. 채집 · 광맥 · 고철 더미 ★★

- **정의**: 약초 · 토양 · 씨앗 · 표본 채집지 + 행성 광맥(E 길게 눌러 채굴).
- **기획 의도**: **전투하지 않고도 할 일**을 맵에 깔았다. 익스트랙션에서 전투 회피는 정당한 전략인데,
  회피만 하면 할 게 없어진다. 채집은 **소리가 나지 않고 시간이 걸리는** 활동이라
  "적을 피해 조용히 캐고 나온다"는 플레이 스타일을 성립시킨다.
  광맥은 **총기 드롭과 같은 등급표**를 쓴다 — 별도 표를 만들지 않아,
  "threat 3 행성에서 캐야 전설·신화가 나온다"는 규칙이 무기와 광물에 동일하게 적용된다.
- **구현**: 광맥은 주로 **언덕 사면**에 붙음(`MINING_HILL_MIN_SLOPE`), 채광 숙련이 등급 보너스.
  행성별 노드 수(3~7)가 데이터로 배정.
- **근거**: `data/planets.csv`, `data/constants.csv`

### D-9. 탐사 차량(로버) ★

- **정의**: 레이드당 1대, 호스트 권한. 탑승자는 무적이며 표적도 아니다. 한 구간당 1명이 요금 지불.
- **기획 의도**: 이동 수단에 **비용**을 붙여 "무료 안전 지대"가 되지 않게 했다.
  탑승 중 무적인 대신 **적과 재해만이 차량에 피해**를 줄 수 있어, 차량 자체가 소모되는 자원이다.
- **근거**: `src/shared/types.ts`(rover 절)

---

# E. 아이템 · 인벤토리 · 경제

> **서술 목표**: 밸런스를 감이 아니라 **구조와 자동 검증**으로 잡았다는 증명. §PORTFOLIO §4와 직결.

### E-1. 디아블로 2식 격자 인벤토리 ★★

- **정의**: 크기가 다른 아이템을 격자에 채우는 가방 + 장비 슬롯 + 퀵슬롯 + 주머니 + 함선 창고.
- **기획 의도**: 격자 인벤토리는 **"무엇을 버릴 것인가"를 공간으로 묻는 장치**다. 무게만 쓰면 산수가 되고,
  격자를 쓰면 **테트리스적 판단**이 된다 — 익스트랙션의 탈출 직전 긴장이 여기서 나온다.
  가방은 **5칸 폭 고정**에 세로만 늘려, 가방 업그레이드가 "면적"이 아니라 "줄 수"로 직관적으로 읽히게 했다.
- **구현**: 가방 8종, 프레임은 가장 큰 가방 기준(패딩 줄은 드롭 불가).
  우클릭 = 아이템 메뉴 / 더블클릭 = 빠른 이동(**장착 중인 아이템을 조용히 밀어내지 않음**) /
  병합 초과분은 커서에 남음.
- **근거**: `src/inventory/README.md`, `data/bags.csv`

### E-2. 퀵슬롯 · 주머니를 "격자 밖 컨테이너"로 분리 ★★

- **정의**: 퀵슬롯과 주머니(구급·열쇠·채집·귀중품 4종)는 가방 격자가 아닌 별도 컨테이너.
- **기획 의도**: 소모품을 가방 격자에 두면 **전투 중 쓸 물건이 전리품 공간을 먹는다**.
  그래서 분리하되, **무게·개수·소모·시체 이전에는 포함**시켰다 — 공짜 공간이 아니라 *용도 전용 공간*이다.
  주머니를 4종으로 고정 1칸씩 준 것도 같은 이유 — 종류별로만 담기므로 "만능 확장 가방"이 되지 않는다.
- **근거**: `CLAUDE.md` §4.7, `src/inventory/README.md`

### E-3. 제작 · 분해 · 수리 경제 ★★★

- **정의**: 아이템의 가치를 `recipes.csv`가 단독 결정하고, 수리비와 분해 수율이 **재료에서 파생**된다.
- **기획 의도**: 제작/분해/수리를 각각 따로 밸런싱하면 **반드시 무한 이익 루프가 생긴다**
  (싸게 만들어 비싸게 분해, 또는 수리보다 새로 만들기가 싸서 수리 기능이 죽는 등).
  그래서 **하나의 원천(제작 재료)에서 나머지 둘을 파생**시켜 구조적으로 불가능하게 만들었다.
  내구도를 5구간으로 나눠, 닳은 장비는 수리비가 오르고 분해 수율은 떨어진다 → **"고쳐 쓸까 분해할까"**가 매번 판단.
- **구현**: `REPAIR_COST_BY_DURABILITY` / `SALVAGE_YIELD_BY_DURABILITY`.
  재료 반감은 **올림, 최소 1**, 내구 장비는 재료 종류당 최소 2 유지.
- **서술 각도**: E-4와 한 세트로 묶어 **"경제를 설계하고, 그 설계가 깨지지 않음을 증명했다"**로.
- **근거**: `data/recipes.csv`, `src/items/Salvage.ts`

### E-4. 경제 무결성 자동 증명 ★★★

- **정의**: `npm run data:check` 실행 시 **"수리 + 분해 ≤ 제작"이 전 아이템에서 성립함을 검사**한다.
- **기획 의도**: 밸런스 붕괴를 **리뷰로 잡는 대신 CI로 잡겠다**는 판단. 아이템이 200종을 넘어가면
  사람이 전수 검사할 수 없고, 한 줄만 고쳐도 파급이 어디까지 가는지 모른다.
- **실제 작동 사례**: 2026-09-16, 제작 재료 환급을 내구 장비까지 확장하려 하자 **66건 위반** 보고
  (반올림 누적 — 수리는 올림이라 `ceil(0.5×3)=2 > 0.65×3=1.95`). → 내구 장비를 환급 대상에서 제외하는 결정.
- **서술 각도**: **면접 답변용 최고의 소재.** "제 결정 하나가 66건의 위반을 만들었고, 제가 만든 검사가 그걸 잡았습니다."
- **근거**: `src/items/Salvage.ts` `checkSalvageEconomy()`, `scripts/data-check.mjs`, `docs/DECISIONS.md 「2026-09-16 — 제작과 숙련」`

### E-5. 제작 숙련 = 재료 환급 (해금도 속도도 아님) ★★★

- **정의**: 제작·의학·원예·연구 숙련이 하는 일은 **소모 재료를 일부 돌려받는 확률** 하나뿐.
- **기획 의도**: 숙련이 제작을 **막으면** 초반 플레이어에게 콘텐츠를 잠그는 벌칙이 되고,
  **빠르게 하면** 체감이 거의 없다. 그래서 "잘하면 이득"만 남겼다.
  환급은 **소모 재료 한 개마다 따로 굴린다** — 한 번에 몰아 굴리면 35개짜리 저격총 제작이
  1개짜리 탄약 제작과 똑같이 '전부 아니면 전무'로 느껴진다. 개별 굴림이면 **큰 제작이 실제로 크게 느껴진다.**
  **내구 장비는 환급 제외**(E-4의 경제 불변식을 깨므로).
- **부가 판단**: `skillRequired` 컬럼과 잠금 UI 배선은 **살려 두되 전 값 0**.
  *"아무도 읽지 않는 컬럼은 나중에 켤 수 있는 스위치가 아니라, 조용히 아무것도 안 하는 숫자"*이기 때문.
  가짜 csv 값으로 잠금 UI가 실제로 뜨는지 검증한 뒤 0으로 복원했다.
- **서술 각도**: **"기능을 줄이는 결정"과 "미래를 위해 배선을 남기는 결정"을 같은 날 함께 내린 사례.**
  판단의 결이 드러나는 최고의 단위.
- **근거**: `docs/DECISIONS.md 「2026-09-16 — 제작과 숙련」`, `src/shared/craftRefund.ts`

### E-6. 전리품 등급 곡선 (서사+ 밀도 제어) ★★

- **정의**: 서사(Epic) 이상 결과는 행성별 `epicPlusMul` 확률로만 유지되고, 실패 시 **서사 미만 최고 등급으로 강등**.
- **기획 의도**: 희귀도 "상한"만 두면 상위 행성에서 전설이 쏟아진다. 반대로 확률만 낮추면
  보장 드랍·폴백 드랍 경로로 새어 나간다. 그래서 **상한이 아니라 밀도**를 제어하고,
  **보장·폴백 포함 모든 경로**에 같은 필터를 걸었다. 연구실 잠긴 방만 예외 —
  **열쇠를 써서 연 방은 보상이 확실해야 하기 때문**.
- **구현**: `data/planet_loot.csv`, `src/items/Loot.ts`, `CrateLootOpts.lockedRoom` 플래그를 모든 상자 경로가 전달.
- **근거**: `CLAUDE.md` §4.7

### E-7. "미리보기 = 실제" 불변식 ★★★

- **정의**: 상자 내용물 미리보기와 실제 개봉 결과가 **반드시 일치**한다. 가젯 배치 미리보기와 실제 배치 판정도 동일.
- **기획 의도**: 플레이어 신뢰는 **한 번 어긋나면 회복되지 않는다**. "초록색이었는데 안 놓였다",
  "미리보기엔 있었는데 열어보니 없다"는 버그가 아니라 **설계 실패**로 취급했다.
  그래서 굴림 결과를 저장하지 않고 **시드 기반 순수 함수**로 양쪽이 같은 값을 계산하게 했고,
  배치는 미리보기와 실제가 **문자 그대로 같은 함수**를 부른다.
- **구현**: `shared/lootRolls.ts`, `gadgets/parts/Preview.ts`.
- **서술 각도**: **"UX 신뢰를 설계 불변식으로 못 박은 사례."** 정보상(§G-3) 상품의 근거이기도 하다.
- **근거**: `CLAUDE.md` §4.7

### E-8. 되돌릴 수 없는 결정의 UX 규약 ★★

- **정의**: 되돌릴 수 없는 확정은 **1초 홀드**, Enter로는 확정 불가, ESC는 취소, 초기 포커스는 `취소`.
- **기획 의도**: 익스트랙션은 실수의 대가가 크다(장비 분해, 창고 이동, 레이드 포기).
  "정말?" 팝업은 **연타로 넘어가므로 아무것도 막지 못한다**. 그래서 **물리적 시간**을 요구했다.
  반대로 **잃을 것이 없는 상황**(함선에서 타이틀로 나가기)은 탭 한 번으로 끝낸다 —
  마찰은 위험한 곳에만 둔다.
- **구현**: `UI_HOLD_CONFIRM_S`, `ui:cursorHold` 링 게이지, ESC 스택 연동.
- **근거**: `CLAUDE.md` §4.2

---

# F. 함선 생활 콘텐츠

> **서술 목표**: "함선 = 두 번째 게임". 볼륨과, **그 볼륨이 레이드로 되돌아가는 연결**을 함께 보여준다.

### F-1. 방 · 가구 · 배치 (발전기 게이트) ★★

- **정의**: 방 용도 11종 · 가구 51종 · 가구 강화 40단계 · 시설 업그레이드 16단계.
- **기획 의도**: 하우징을 **꾸미기**가 아니라 **해금 트리**로 만들었다. 방 용도마다 요구 발전기 레벨이 다르므로
  (작업실 1 · 온실 2 · 연구실 3 · 헬스장/서재 4 · 채굴실 5), **발전기가 곧 진행도**다.
  "전력 배분" 시스템은 **일부러 폐기**했다 — 방마다 전력을 나누는 미니게임은 매번 같은 답이 나오는 계산 노동이 된다.
  대신 **레벨 게이트 하나로 단순화**하고, 그 자리에 콘텐츠를 더 넣었다.
  가구는 **접근 면(access face)** 개념이 있어 벽에 붙이면 못 쓰는 등, 배치가 실제 판단이 된다.
- **구현**: `data/room_purposes.csv`(증축 비용 + 요구 발전기) · `furniture.csv` · `furniture_upgrades.csv`.
  요구 레벨 미달 시설은 로드 시 제거되고 **재료를 전액 환불**(마이그레이션 v13).
- **근거**: `src/housing/README.md`, `docs/DECISIONS.md 「2026-09-13 — 발전기」`

### F-2. 온실 (토양 → 씨앗 → 수확) ★★★

- **정의**: 흙을 붓고 → 씨앗을 심고 → 수확. 흙에는 **내구도**와 **소켓**이 있다.
- **기획 의도**: 재배를 "심고 기다리기"로 끝내지 않고 **소모 자원**으로 만들었다.
  흙은 수확할 때마다 닳고(등급별 2·3·5회), 닳으면 다시 레이드에서 퍼 와야 한다 →
  **함선 콘텐츠가 레이드 동기를 계속 재생산**한다(§A-1의 엔진).
  씨앗은 **행성마다 나는 품종이 다르고**, 고급 품종은 채집으로 안 나오고 **분석기 해석으로만** 나온다(§F-3)
  → 생활 콘텐츠끼리도 물려 있다.
- **구현**: 토양 등급별 사용 횟수 + **태그 매칭**(맞는 흙에 맞는 씨앗) + 소켓 슬롯 수는 흙 등급이 결정.
  스테이션 업그레이드 시 **자라던 작물을 비율로 재조정**(진행 중 손실 없음).
- **근거**: `src/housing/parts/Garden.ts`, `data/seeds.csv`, `docs/DECISIONS.md 「2026-09-11 — 온실 개편」`

### F-3. 연구실 분석기 + 표본 17종 ★★★

- **정의**: 미확인 표본을 넣고 실시간 시간을 기다리면 해석 결과가 나온다.
- **기획 의도**: **오프라인 진행(실시간 대기)**을 넣되, "기다리면 공짜"가 되지 않게 표본을 레이드에서만 얻게 했다.
  핵심 설계는 **표본 등급 = 산출물의 바닥**이라는 규칙이다(천장이 아니다).
  「미확인 유전자 III(희귀)」를 해석하면 **희귀 이상만** 나온다 →
  낮은 등급은 운이 좋으면 위쪽도 뽑고, 높은 등급은 **아래쪽이 후보에서 아예 빠진다**.
  "상위 표본을 넣었는데 쓰레기가 나왔다"는 좌절을 구조적으로 제거한 것이다.
  3계열이 **여는 문을 나눠** 각 계열의 존재 이유를 분리했다:
  유전자 → 토양·배양조 / 세포 → 배양조 / **광물 → 무기**.
- **구현**: 17종(세포 6등급 · 광물 6등급 · 유전자 5등급).
  수치 곡선을 **두 줄로 요약 가능하게** 설계 — `value = 60 × 2^(등급−1)`, `analyzeHours = 1·2·4·6·9·12`.
  세 계열이 같은 곡선을 쓰고, 계열은 "무엇이 나오는가"만 가른다.
- **서술 각도**: **"밸런스 곡선을 공식 한 줄로 만들었다"**는 것 자체가 강한 어필이다.
  표를 손으로 채우면 반드시 들쭉날쭉해지고 확장할 때마다 다시 손봐야 한다.
- **근거**: `data/samples.csv`(머리말에 설계 전부 서술), `data/analysis_results.csv`, `src/housing/parts/Lab.ts`

### F-4. 소켓 — 영구 강화와 되돌릴 수 없음 ★★

- **정의**: 흙·배지에 끼우는 영구 강화 인자 16종(속도 / 수확량 / 내구). **분석기 해석으로만** 획득.
- **기획 의도**: 함선 시설에 **장기 투자 축**을 만들되, 되돌릴 수 있으면 투자가 아니다.
  그래서 **한 번 끼우면 빠지지 않고, 덮어 끼우면 옛 소켓이 파괴된다.**
  대신 효과가 **그 흙의 내구도 비율만큼만** 작동하게 해서(닳으면 효과도 줄어듦),
  "좋은 소켓을 싸구려 흙에 박는" 낭비를 자연스럽게 억제했다.
  단, 내구를 지키는 소켓(`wear`)만은 **비율 영향 없이 그대로** 작동한다 — 그 소켓의 존재 이유가 내구 보존이므로.
- **근거**: `data/sockets.csv`(주석에 규칙 전문)

### F-5. 배양조 (배지 → 스캐폴드 → 세포주) ★★

- **정의**: 3단 구조의 배양 시설. 배양육·유단백·치즈 등 요리 상위 재료 생산.
- **기획 의도**: 요리(§F-6)의 **상위 재료 공급선**을 별도 시설로 분리했다.
  이유는 두 가지 — ① 요리 재료가 전부 채집이면 레이드 반복만 남고,
  ② 상위 요리에 **시간 투자**를 요구해야 최상위 식사 버프가 값져진다.
  온실과 **같은 소켓·내구 구조**를 쓰게 해서 학습 비용을 재활용했다(새 시스템을 배울 필요 없음).
- **근거**: `src/housing/parts/Culture.ts`, `data/analysis_results.csv`

### F-6. 요리 — 미니게임 6종 → 품질 → 식탁 접시 ★★★

- **정의**: 썰기·다지기·굽기·볶기·젓기·붓기 6종 리듬 미니게임 → 품질 판정 → **식탁 위 접시**로 완성.
- **기획 의도**: 이 시스템의 핵심 판단은 **"식사는 아이템이 아니다"**이다.
  아이템이면 창고에 100개 쌓아 두고 레이드마다 먹으면 되고, 그러면 요리가 **작업**이 된다.
  대신 **개인 함선에 접시 하나**만 존재하고, 새로 요리하면 이전 접시를 덮어쓰며(1초 홀드 경고),
  먹어도 사라지지 않고 **레이드를 시작하면 사라진다**.
  → "이번 출격에 무엇을 먹고 나갈 것인가"라는 **출격 전 단 하나의 선택**이 된다.
  공유 함선에는 **분대원 각자의 접시**가 식탁에 함께 놓여, 요리를 잘하는 사람이 분대에 기여하는 그림이 나온다.
  식당 가구가 없으면 요리대도 못 쓴다 — **먹을 자리 없이 요리하지 않는다**는 공간 논리.
- **구현**: 식사 25종(등급 4단계), 효과는 전부 `DerivedStats` 필드로 합류(§B-3).
  **미니게임 난이도 규칙**: 화면에 그려진 마커 크기가 **곧** 완벽 판정 폭이다 —
  난이도는 판정 폭이 아니라 **박자 속도**로 올린다(보이는 것과 판정이 어긋나지 않게).
- **서술 각도**: **"식사를 아이템에서 빼낸 결정"** 하나로 요리 시스템 전체의 성격이 정해진 사례.
- **근거**: `data/meals.csv`, `data/cook_steps.csv`, `src/housing/parts/Cooking.ts`, `CookGames.ts`

### F-7. 서재 — 매체 4종 · 시리즈 68종 ★★

- **정의**: 책·디스크·레코드·게임을 책장에 꽂으면 효과가 붙는다. 시리즈 단위로 완성 보너스.
- **기획 의도**: **수집 콘텐츠를 성장에 연결**하는 고전적 장치인데, 두 가지를 비틀었다.
  ① **행성별로 나오는 시리즈가 다르다** → 도감을 채우는 것이 §A-1의 행성 선택 동기가 된다.
  ② **각 권은 한 번만 계산된다** → 같은 책 10권을 쌓는 꼼수를 원천 차단.
  책 제목 자체가 게임 세계관 서술이다(『짐꾼의 요령』 『감정사의 현장 노트』 『사격 교본: 산탄총』 —
  **제목이 곧 그 책이 올리는 숙련**이라 UI 설명 없이 읽힌다).
- **구현**: `data/library_series.csv` 68행, 매체별 책장 가구, on/off 토글, 효과 캐시.
- **근거**: `data/library_series.csv`, `src/housing/parts/Library.ts`

### F-8. 헬스장 · 비디오게임 — 단련 ★

- **정의**: 리듬 미니게임 3종(프레스·호흡·사이클) → 단련 스탯(스탯 포인트와 별개).
- **기획 의도**: **레벨업 포인트 없이 능력치를 올릴 수 있는 경로**를 하나 뒀다.
  단, 피로도를 붙여 무한 반복을 막았다. 비디오게임(TV+콘솔+디스크)은 **같은 규칙을 재사용**하되
  획득처가 레이드 전리품이라 수집 콘텐츠와 연결된다.
- **근거**: `src/housing/parts/Gym.ts`, `VideoGame.ts`, `data/game_discs.csv`

### F-9. 암호화폐 채굴 · 거래 ★★

- **정의**: 코인 8종. 프로세서를 꽂은 연산 클러스터가 실시간으로 채굴, 릴레이 권위 시세로 거래.
- **기획 의도**: **완전히 다른 리듬의 수익원**을 하나 두고 싶었다 —
  레이드가 "위험을 감수하고 즉시 얻는" 수익이라면, 채굴은 **"켜 두고 잊는" 수익**이다.
  대신 세 가지 마찰을 붙였다: ① 프로세서가 **닳는다**, ② 코인마다 주기·변동성이 다르다
  (스크랩 12h/변동 4% ~ 보이드 36h/변동 16%), ③ **기업 코인 4종은 NPC 채굴 인가 퀘스트로 해금**.
  시세는 **서버 권위**라 클라이언트 조작이 불가능하고, 거래는 크레딧 사유로 검증된다.
- **구현**: `data/crypto.csv` 8행, 코어 수에 따른 주기 배수, 1 Hz 정산(레이드 중엔 정지), 차트는 항상 보임(해금 전에도).
- **근거**: `data/crypto.csv`, `src/housing/parts/Mining.ts`, `shared/cryptoMarket.ts`

---

# G. 메타 · 기업 · 서사

### G-1. 기업 4곳 · 신뢰도 · 상점 ★★

- **정의**: 헬릭스 방산(돌격/기관/권총·경량탄) · 바스티온 중공업(저격/지정사수/산탄·중량탄) ·
  노마드 장비(가방·방탄복·수류탄) · 세레스 바이오(회복·임플란트·수리).
- **기획 의도**: 상점을 하나로 두면 **"돈만 모으면 된다"**가 된다. 취급 품목을 4곳으로 갈라
  **"어느 기업의 신뢰를 쌓을 것인가"**를 빌드 선택으로 만들었다.
  신뢰도 게이트를 품목별로 걸어(전술 가방은 노마드 신뢰 3부터, 부착물은 세레스 2부터)
  진행 순서에 굴곡을 줬다. 각 기업에 **한 줄 슬로건**을 붙여 성격이 즉시 읽히게 했다
  ("수량이 곧 화력" / "한 발의 무게" / "짊어진 만큼 살아 돌아온다" / "몸이 먼저다").
- **근거**: `data/corps.csv`, `data/corp_stock.csv`, `src/meta/README.md`

### G-2. 계약 26종 — 플레이 스타일별 목표 ★★

- **정의**: 기업별 신뢰도 단계에 따라 열리는 반복 목표.
- **기획 의도**: 계약을 **기업 성격에 맞춰 갈랐다** — 헬릭스는 벌레 처치, 바스티온은 인간형 처치,
  노마드는 **전리품 가치를 들고 탈출**, 세레스는 **시체 루팅**. 즉 계약이
  "어느 기업을 밀 것인가 = 어떻게 플레이할 것인가"가 된다.
  회수 계약은 **그 레이드에서 발견한 아이템만** 인정한다(`raidFound` 표식, 섞인 스택은 표식 상실) —
  창고에서 꺼내 제출하는 우회를 막되, 규칙을 데이터 구조로 못 박았다.
- **근거**: `data/contracts.csv`, `shared/raidFound.ts`

### G-3. 정보상 레이븐 — 레이드 기믹 구매 ★★★

- **정의**: 출발 전 크레딧으로 **그 레이드의 구성을 고정**하는 상품 7종.
- **기획 의도**: 절차 생성의 불확실성은 재미의 원천이지만, **동시에 스트레스의 원천**이다.
  그래서 불확실성을 **돈으로 줄일 수 있는 선택지**를 만들었다 — 이게 크레딧의 새로운 소비처가 되고,
  동시에 "이번엔 확실하게 가자"는 **출격 전 전략 단계**를 만든다.
  상품 설계가 핵심이다: 탈출 지점 +n(위협 3에서 가장 값짐) · 지하 시설 +n(열쇠 필요하다고 명시) ·
  기상 예보(재해 지연) · 궤도 운행(선로 확정) · 탐사 차량 확정 · **현상 수배(지정 네임드 확정, 2,600 C)** · 벌레 둥지 +n.
  **둥지 +n은 양날**이다 — 표본·재료가 늘지만 압박도 는다. "이득만 주는 상품"을 두지 않았다.
  가격은 단계·묶음·행성 위협도에 따라 곱해져서, **비쌀수록 확실해지는** 곡선을 만든다.
  **분대장만 구매 가능**하고 **환불 없음** — 분대의 공동 자원을 쓰는 결정에 책임을 붙였다.
- **구현**: `data/intel_options.csv`. 단계→실제 보너스 대응은 **코드에 둔다**
  ("탈출구 +2라고 써 놓고 다른 수를 주면 화면이 거짓말을 하므로, 이 표는 정의이지 밸런스가 아니다").
- **서술 각도**: **"절차 생성의 불확실성을 상품화했다"** — 시스템 간 연결 설계의 모범 사례.
- **근거**: `data/intel_options.csv`, `docs/DECISIONS.md 「2026-09-14 — 정보상」`

### G-4. NPC 10인 · 퀘스트 42 · 메신저 ★★

- **정의**: 기업별 실무자/임원 8명 + 독립 2명(정보 브로커 레이븐 · 레이더 탈영병 케인).
  퀘스트 42개 / 목표 96개. 메신저 UI로 수신.
- **기획 의도**: 퀘스트를 **게시판이 아니라 사람**에서 나오게 했다. 각 NPC가 자기 직무에 맞는 것을 요구한다 —
  탄도 시험관은 "지정사수소총 명중 기록", 수선공은 "실전에서 굴러 본 총기", 조달실장은 "경쟁사 동향 확인".
  즉 **퀘스트 내용이 곧 캐릭터 소개**다.
  구조적 판단 두 가지: ① **첫 연락은 함선에서만** (레이드 중 대화창이 뜨지 않게),
  ② **수락한 퀘스트는 포기 불가** (골라 담기 방지 — 받기 전에 고민하게 만든다).
  기업 코인 해금 퀘스트(`q_*_permit`)를 각 기업 **임원**에게 붙여, 채굴 콘텐츠(§F-9)가
  기업 신뢰도 진행과 물리게 했다.
- **구현**: `data/npcs.csv` / `npc_quests.csv` / `npc_objectives.csv`. 요구 조건은
  레벨·기업 신뢰도·NPC 개인 신뢰도·선행 퀘스트·진행 플래그. 퀘스트 id가 곧 크레딧 원장 키.
- **근거**: `data/npc_quests.csv`, `src/meta/parts/NpcQuests.ts`

---

# H. 협동 · 온보딩 · 접근성

### H-1. 안드로이드 분대원 ★★★

- **정의**: 릴레이의 **봇 멤버**로 분대 슬롯을 채우는 AI 동료. 사람이 들어오면 **가장 최근 안드로이드가 자동 퇴장**.
- **기획 의도**: 4인 협동 게임의 최대 진입장벽은 **"같이 할 사람이 없다"**이다.
  AI 동료를 넣되, **별도 솔로 모드를 만들지 않는 것**이 핵심 판단이었다 —
  분대 슬롯을 그대로 쓰게 하면 콘텐츠가 하나뿐이고, 사람이 오면 자연스럽게 교대된다.
  행동 설계에서도 "사람 흉내"를 목표로 잡았다: 반응 지연이 있는 FSM,
  분대장 주위 **하니스** 안에서는 관심 지점을 찾아 **배회**하고 벗어나면 뛰어 돌아온다,
  서로 일정 거리를 유지하고 사람에게 다가갈 때 흩어진다.
  **명령 권한을 나눈 것**도 의도적이다 — "가자/주의/앞장"은 **분대장만**,
  하지만 **적·탈출 핑은 아무 사람에게나** 반응한다(분대장이 아니어도 위험은 알릴 수 있어야 하므로).
  **레이드 실패 조건에 안드로이드를 포함**시켜(서 있는 안드로이드는 구조하러 온다),
  동료가 장식이 아니라 실제 생존 자원이 되게 했다.
- **구현**: 기본 장비는 **바인딩**(버리거나 주거나 시체에 남기거나 창고에 넣을 수 없음), 함선에서도 유지.
  레이드에서 주운 것만 이동 가능. 조종석 안드로이드 베이 3개(3초 홀드로 영입/해고).
- **서술 각도**: **"솔로 모드를 만들지 않고 솔로 문제를 푼 방법"**으로 서술하면 구조 설계 감각이 드러난다.
- **근거**: `src/allies/README.md`, `docs/DECISIONS.md 「2026-09-15 — 안드로이드 분대원」`

### H-2. 분대 커뮤니케이션 (핑 · 채팅 · 메신저) ★★

- **정의**: 플레이어별 핑 3개 + 확인 핑 + 화면 밖 화살표 + 전투불능 전용 문구. 함선 메신저.
- **기획 의도**: 음성 채팅 없이도 4인이 굴러가야 한다.
  **핑 개수를 사람당 3개로 제한**한 이유는 도배 방지가 아니라 **핑의 의미를 지키기 위해서**다 —
  무제한이면 화면이 마커로 덮이고 아무도 안 본다.
  **확인 핑**(다른 사람의 핑에 동의)을 넣어 "봤다"는 응답이 가능하게 했다.
  전투불능 상태에서는 **전용 문구**가 나가서, 쓰러진 사람이 타자를 칠 수 없어도 의사가 전달된다.
  탄약 요청은 **인벤토리 가운데 클릭으로만** — 남의 가방을 열어 보는 UI를 만들지 않으면서도 요청이 가능하다.
- **구현**: 화면 밖 핑은 나침반 눈금 + 화살표. 메신저 버튼은 Tab 창과 일시정지 메뉴 위에서도 동작
  (연락은 언제든 받을 수 있어야 하므로).
- **근거**: `src/ui/README.md`, `docs/DECISIONS.md 「2026-09-09 — 핑 v3」`

### H-3. 튜토리얼 3트랙 ★★★

- **정의**: ① 레이드 트랙(전용 수작업 행성) ② 함선 트랙 ③ 건설 트랙. **각각 따로 건너뛸 수 있다.**
- **기획 의도**: 가장 중요한 결정은 **"게임이 레이드에서 시작한다"**는 것이다.
  익스트랙션 슈터의 첫인상은 전투와 탈출이어야 하는데, 보통은 로비/상점 설명부터 시작해서
  **장르의 재미를 맛보기 전에 이탈**한다. 그래서 새 캐릭터는 **깨어나자마자 레이드 한복판**이다.
  트랙을 셋으로 나누고 **각각 건너뛰기**를 준 이유는, 2번째 캐릭터를 만드는 사람에게
  "전부 아니면 전무"를 강요하지 않기 위해서다. 건너뛰기는 **즉시 탈출** 처리라 벌칙이 없다.
  구현 철학도 중요하다 — 튜토리얼은 **다른 폴더를 건드리지 않는다.**
  각 시스템이 자기 "왜 안 되는가" 함수 안에서 `ctx.tutorial?.blockReason()`을 물어볼 뿐이라,
  튜토리얼을 통째로 들어내도 게임이 멀쩡하다.
  **체크포인트가 체력이 안 찬 상태로 넘어가지 않게** 하는 등, 실패 상태로 다음 단계에 진입하는 경로를 막았다.
- **구현**: 목표 패널 · 스포트라이트 · 교체식 조작 가이드 · 순서 강제 게이트. 진행은 **버스 이벤트 관찰로만** 판정.
- **서술 각도**: **"온보딩 설계"는 콘텐츠 기획 직무 그 자체다.** 단독 챕터로 쓸 것.
- **근거**: `src/tutorial/README.md`, `docs/DECISIONS.md 「2026-09-14 — 튜토리얼 개편」`(1~4차 결정 기록)

---

# 부록 1. 우선순위 요약 — 지면이 부족할 때

**★★★ 20개 중 최상위 8개** (상세 포트폴리오 12p 기준, 각 1페이지):

| 순위 | 단위 | 한 줄로 팔 문장 |
|---|---|---|
| 1 | A-1 함선→행성 루프 | "왜 저 행성인가"의 답을 레이드 밖에서 만들었다 |
| 2 | A-2 손실 설계 | 벌칙의 총량이 아니라 성질을 바꿨다 |
| 3 | E-4 경제 자동 증명 | 밸런스 붕괴를 리뷰가 아니라 CI로 잡는다 |
| 4 | E-5 제작 숙련 | 기능을 줄이는 결정과 배선을 남기는 결정을 같이 내렸다 |
| 5 | C-5 함선 지원 공유 쿨다운 | 제약을 늘려 선택을 만들었다 |
| 6 | G-3 정보상 | 절차 생성의 불확실성을 상품으로 만들었다 |
| 7 | H-1 안드로이드 분대원 | 솔로 모드를 만들지 않고 솔로 문제를 풀었다 |
| 8 | H-3 튜토리얼 3트랙 | 게임이 로비가 아니라 레이드에서 시작한다 |

**보조 추천**: F-3(분석기 — 곡선을 공식으로) · F-6(요리 — 식사를 아이템에서 뺌) ·
C-2(유니크 — 보상을 숫자가 아니라 동사로) · D-2(재해 — 피드백→수치 개선 기록).

---

# 부록 2. 서술 템플릿 (각 단위를 글로 풀 때)

```
■ <단위 이름>

[문제]   이 장르/이 게임에서 무엇이 문제였나  (1~2문장)
[판단]   그래서 무엇을 선택했나 · 무엇을 일부러 안 했나  (1~2문장)
[구현]   실제로 들어간 것 — 수치와 구성  (표 또는 3~5줄)
[효과]   플레이어 경험이 어떻게 달라지는가  (1문장)
[확인]   저장소 경로 또는 스크린샷
```

**주의**: `[판단]`에 **"무엇을 안 했는가"를 반드시 넣을 것.**
기각한 안이 적힌 서술이 훨씬 신뢰받는다 (`docs/DECISIONS.md`의 "Rejected" 항목이 소재).

---

# 부록 3. 데이터 정합성 메모

포트폴리오에 수치를 옮기기 전 확인할 것:

- `docs/pitch/pages/28-status.html`의 현황표가 **일부 낡았다** — 숙련도 `14` → 실제 **16**,
  가젯 `10종` → 아이템 기준 **13종**, 암호화폐 "다음 단계" → **구현 완료**,
  튜토리얼 `17단계` → **3트랙 구조로 개편됨**. 제출 전에 이 페이지를 갱신할 것.
- `data/enemies.csv` 22행에는 **튜토리얼 전용 4종과 약화 개체 1종이 포함**된다.
  외부 서술은 "적 17종 + 튜토리얼 전용 4"처럼 분리해서 쓰는 편이 정확하다.
- `data/items.csv` 144행은 **총기를 제외한** 아이템이다(총기는 계열 5 × 등급 × 희귀도로 생성).
  "아이템 144종 + 생성 총기 + 임플란트 아이템 + 서재 매체 68"처럼 층을 나눠 표기할 것.
