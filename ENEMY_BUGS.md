# 벌레(터미니드) 종류 정리

`data/enemies.csv` 의 `faction=bug` 종류 + 튜토리얼 전용 2종을 한자리에 모은 **읽기용 요약**이다.
수치의 원본은 항상 csv 이고 이 문서는 사본이다 — 값이 어긋나면 csv 가 맞다.

- 기본 스탯: `data/enemies.csv`
- 특수 능력: `data/enemy_abilities.csv` (`HUNTER_LEAP` · `SPEWER_SPIT` · `CHARGER_CHARGE` · `ARTILLERY_AI` · `TOXIC_AI` · `BEHEMOTH_AI`)
- 상수식(`=0.8*BEHEMOTH_SCALE` 등) · 땅굴벌레 이벤트: `data/constants.csv` (`BEHEMOTH_SCALE` · `TOXIC_*` · `SANDWORM_*`)
- 둥지 규칙(리시 · 수비대 · 보충): `data/tables.csv` 의 `NEST_*`
- 행성별 구성: `data/planets.csv` 의 `bugs` 열, 위협도 배수는 `data/tables.csv`
- 한국어 이름: `src/meta/NpcRules.ts` 의 `ENEMY_TYPE_KO`
- 스냅숏 시점: 2026-09-18

---

> **2026-09-17 전면 재조정**: 플레이어가 적에게 주는 피해와 **모든 적 hp 를 1/3 (내림)** 로 줄였다 — 아래 표의 HP 는 그 뒤의 csv 값이다.
> 적이 플레이어에게 주는 피해는 그대로이고, `raidXp` 도 옛 값(hp/10 시절)을 그대로 쓴다 — 이제 hp 와 raidXp 는 따로 논다.
> 적이 **다른 적**에게 주는 피해만 `ENEMY_CLASH.damageMul`(1/3)을 곱한다 (재해 피해는 예외).

## 1. 소형 — 수로 압박

| 종류 | type | HP | 속도 | 근접(피해/쿨) | 특징 |
|---|---|---|---|---|---|
| 스캐빈저 | `scavenger` | 40 | 5.5 m/s | 4 / 0.8 s | 기본 잡몹. 다섯 행성 전부에 가장 높은 가중치로 깔린다 |
| 헌터 | `hunter` | 120 | 7.5 m/s | 7 / 1 s | **도약** 10~18 m · 22 피해 · 체공 1.55 s · 명중 반경 2.4 m · 쿨 12 s · 포물선 중력 ×0.5. 가장 빠르다. 2026-09-17: 한 도약 동안 **20 피해**를 받으면 그 자리에서 수직으로 떨어져 3 s 뒤집혀 눕는다 |
| 독성 버그 | `toxic` | 46 | 7.2 m/s | 없음(자폭) | 2.2 m 안에 들면 0.6 s 부풀고 반경 5 m · **60 피해**로 터진다. `staggerFraction 1.1` = 경직에 안 걸린다 |
| **벌레 알** | `bug_egg` | 40 | **0 (고정)** | 없음 | 2026-09-18 신규 — 둥지 밑동의 부술 수 있는 알. 아래 4절 |

## 2. 중형 — 전열

| 종류 | type | HP | 속도 | 근접(피해/쿨) | 특징 |
|---|---|---|---|---|---|
| 워리어 | `warrior` | 213 | 4.5 m/s | 12.5 / 1.2 s | 정직한 근접 탱커 |
| 스퓨어 | `spewer` | 173 | 3.2 m/s | 6 / 1.5 s | **산성 침** 8~22 m · 18 + 스플래시 10 · 둔화 2 s · 예비동작 0.55 s, **사망 폭발** 반경 4 m · 20 |

> 2026-09-16: 땅굴벌레 · **벌레 알**을 뺀 모든 벌레가 **발소리**를 낸다 (`stepSound`) — 벌레 전용 소리 3종(소형 갑각 톡톡 · 중형 쿵 · 베헤모스 땅울림), 22 m (베헤모스 34 m) 안에서만,
> 여러 마리가 겹치면 1/√n 감쇠 · 동시 6개. 2026-09-18 에 이 소리들의 크기를 올렸다 (오디오 요청 — 값은 `model.STEP_VOICES`).
> 처치 경험치는 csv `raidXp` (스캐빈저 12 · 헌터 36 · 워리어 64 · 스퓨어 52 · 차저 180 · 포격 84 · 독성 14 · 베헤모스 280 · 땅굴벌레 250 · 어린 개체 75 · **벌레 알 4** · 포병 소환 스캐빈저 **0**).

## 3. 대형 · 특수

| 종류 | type | HP | 속도 | 근접(피해/쿨) | 특징 |
|---|---|---|---|---|---|
| 차저 | `charger` | 600 | 4 m/s | 15 / 2 s | **돌진** 7~30 m · 예비 0.8 s · 14 m/s · 45 피해 · 최대 3.5 s. 부위 배수 후면 ×2.5 / 정면 ×0.5 — 뒤를 쳐야 한다 |
| 포격 버그 | `artillery` | 280 | 3.4 m/s | 없음 | 땅에 파고들어 **간접 사격**. 사거리 98 m · 6~9 s 간격 · 굴착 1.2 s · 후퇴 42 m / 접근 88 m. 시야 130 m. 궤적이 막히면 자리를 옮기고, 3번 거절되면 8 s 쉰다. 호위 · 포격 준비는 아래 5절 |
| 베헤모스 | `behemoth` | 933 | 3.6 m/s | 22.5 / 2.2 s | 스케일 ×3(`BEHEMOTH_SCALE`) = 반경 2.4 m · 키 4.8 m. 관통 돌진(교전 18 m · 쿨 4 s · 관통 6 m), **다른 팩션 적도 53 피해로 치어버린다**. 후면 ×2 / 정면 ×0.35 |
| 땅굴벌레 | `sandworm` | 666~1000 (호스트가 굴림) | 0 (고정) | 없음 | 레이드 이벤트 보스 (위협 2–3). 아래 6절 |
| 어린 땅굴벌레 | `sandworm_weak` | 250 (고정) | 0 (고정) | 없음 | 위협 1 행성의 땅굴벌레. 몸 · 분출 반경 70 %, 스캐빈저만 뱉는다 |
| 소환 스캐빈저 | `scavenger_summon` | 40 | 5.5 m/s | 4 / 0.8 s | 포병이 제 부대로 불러내는 스캐빈저. 수치 · 리그 · 소리는 스캐빈저 그대로, **드롭 0 % · 처치 경험치 0** (반복 처치로 벌지 못하게) |

> `sandworm` 의 csv hp 833 은 자리표시자다. 실제 최대 체력은 `SANDWORM_HP_MIN..MAX`(666~1000) 에서 호스트가 굴려 분대 전원에 보낸다.
> `sandworm_weak` 의 csv 줄은 `=1*SANDWORM_WEAK_HP` · `=2.2*SANDWORM_WEAK_SCALE` 같은 식이라 상수 하나로 따라간다.

> **2026-09-18 — 폭발 · 근접이 지형을 뚫지 않는다**: 폭발 피해는 폭심에서 몸의 발 · 가슴 · 머리 세 점 중 하나라도 보여야 들어가고
> (`shared/explosion.blastReachesBody`), 근접 공격은 공격자의 머리에서 잰다. 벽 · 지붕 · 바닥 너머의 적 · 플레이어 · 안드로이드 · 차량은
> 맞지 않는다 (땅굴벌레 분출은 예외). 같은 날 **창유리도 막는다** — 깨진 창틀도 폭발 가시선은 통과시키지 않으므로, 창 밖에서 터진
> 포탄 · 수류탄이 방 안의 몸을 때리지 않는다. 낮은 엄폐물은 그대로라 「엄폐를 넘으면 맞는다」 는 유지된다.

## 4. 벌레 알 · 둥지 (2026-09-18 사용자 결정, `src/enemies/NestDirector.ts` · `ai/NestLeash.ts` · `models/EggModel.ts`)

둥지 밑동의 **알 자루가 장식이 아니라 적**이 됐다. 같은 결정으로 **둥지 둘레의 상자 고리가 없어졌다** — 예전에는 둥지마다
상자가 둘려 있었지만 이제 둥지 반경 안에는 상자가 서지 않는다. **둥지의 보상은 알이다.**

### 벌레 알 `bug_egg`

| 항목 | 값 |
|---|---|
| HP | 40 (스캐빈저와 같다) · 행성 위협 배수(`BUG_HP_MUL_BY_THREAT`)는 그대로 받는다 |
| 이동 · 회전 · 공격 · 감지 | **전부 0** — 움직이지 · 돌지 · 공격하지 · 알아채지 않는다. 쏴도 깨어나지 않는다 |
| 경직 · 넉백 | **면역** (`staggerFraction 99` · `mass 1000` — 땅굴벌레와 같은 값) |
| 발소리 | 없음 (`stepSound false`) |
| 몸 크기 | 자리마다 다르다 — 보이는 알 반지름 0.35~0.7 m 가 그대로 히트박스다 (csv 줄은 기준 0.5 m) |
| 처치 경험치 | 4 (= 이 줄 hp / 10). 맞서 싸우지 않는 표적이라 스캐빈저(12)의 1/3 |
| 수 | 둥지 둔덕마다 2~5개 — 자리는 `world/Nests` 가 정한다 (훈련장 · 튜토리얼에는 없다) |

- **드롭**: 생체 조직 1~2개 확정(`loot_corpses.csv`) + **미확인 세포** 80 % 1개 (`loot_corpse_samples.csv`, 등급 I 60 % / II 40 %).
  세포 확률 0.8 은 **헌터(0.6) 와 워리어(1) 사이**다 (사용자 결정). 시체는 늘 수색된다 (`CORPSE_LOOT_CHANCE` 1).
- **재해에 깨지지 않는다** — 지나가는 재해 구역의 조용한 피해(`HAZARD_ENEMY_DPS`)에서만 빠진다. 불 · 소이 지대처럼 **누가 낸**
  피해는 그대로 들어간다.
- 「살아 싸우는 몸」을 세는 자리에는 **하나도 들지 않는다**: 순찰 · 웨이브 인원 상한, 재활용 대상, 포병의 사격 지원 판정
  (알 옆에 서 있다고 포격이 열리지 않는다), 다른 팩션의 표적 고르기, 소리 조사, 아래 둥지 보충의 생존 수.
- 겉모습은 옛 장식 알 그대로다 (같은 타원체 · 같은 그러데이션 · 같은 노른빛). 부술수록 껍질에 어두운 파열구가 하나씩 벌어지고,
  부서지면 주저앉아 땅으로 가라앉는다.

### 둥지 규칙 (`data/tables.csv` 의 `NEST_*`)

- **리시 60 m** (`NEST_LEASH_M`): 둥지에서 태어난 벌레는 둥지에서 60 m 밖으로 나가면 표적을 놓고 걸어 돌아간다.
  돌아가는 동안에는 무슨 일이 있어도 다시 달려들지 않고, **리시 안에 있을 때만** 평소 감지 거리로 표적을 다시 잡는다
  (둥지까지 따라가면 다시 붙는다). 경계에서 잡았다 놓았다 하지 않게 30 m(`NEST_LEASH_RETURN_FRAC` 0.5) 안에 들어와야 돌아가기가 끝난다.
  **둥지에서 나지 않은 벌레**(레이드 중 순찰 · 탈출 웨이브 · 레이더 강하 · 땅굴벌레가 뱉은 벌레)는 예전처럼 끝까지 쫓아온다.
- **초기 수 절반** (`NEST_INITIAL_GARRISON_MUL` 0.5): 레이드 시작 때 둥지 둘레에 까는 순찰 **무리 수**가 절반이다.
  무리 하나의 구성표는 건드리지 않는다 (수만 줄인다).
- **보충 1~3회** (`NEST_REFILL_COUNT_CHANCE`): 둥지마다 이번 레이드에 할 수 있는 보충 횟수를 월드 시드로 한 번 굴린다 —
  **1회 50 % · 2회 35 % · 3회 15 %** (3회는 일부러 드물다). 그 둥지의 살아 있는 **움직이는** 벌레(알은 안 센다)가 처음 깔린 수의
  약 1/3(`NEST_REFILL_TRIGGER_FRAC` 0.34) 이하로 줄면 보충 한 번이 터져 무리 하나가 둥지에서 **파고 나온다**(순찰과 같은 굴착 길).
  검사는 2 s 마다(`NEST_REFILL_CHECK_S`). 배정된 횟수를 다 쓰면 그 둥지는 레이드가 끝날 때까지 비어 있다.
- 전부 호스트(싱글 = 자기 자신)의 결정이고 **새 와이어가 없다** — 분대원은 평소의 적 스폰 메시지로만 본다.
  호스트가 바뀌면 리시 기억이 풀려 그 벌레들은 평범한 벌레가 된다 (의도).

## 5. 포병 버그의 호위 · 포격 준비 (2026-09-17 · 2026-09-18 사용자 결정, `ai/ArtilleryPack.ts` · `ai/GimmickAI.ts`)

- **굴착 호위**: 포병이 파고 나올 때 스캐빈저 2~3마리가 함께 나와 곁을 지킨다 (`escortMin/Max`). 6 m(`escortFollowDist`) 보다
  멀어지면 뛰어 돌아오고, 표적을 스스로 알아채면 평범한 스캐빈저처럼 달려든다.
- **사격 조건**: 표적에게서 10 m(`supportRadius`) 안에 **포병이 아닌 살아 싸우는 벌레**가 하나라도 있어야 쏜다. 외톨이 표적에게는
  사거리 안이어도 쏘지 않는다. **벌레 알은 여기 들지 않는다.**
- **포격 준비** (2026-09-18 신규): 지원이 붙은 **첫 발 앞에서** 2.5 s(`prepTime`) 동안 납작 엎드려 준비한다 — 비명 · 배 꿀렁임 ·
  엎드림 자세가 전조다. 준비 중에 조건이 깨지면(표적이 쓰러짐 · 사거리 밖 · 곁의 벌레가 사라짐) 준비가 취소되고 다음에 처음부터 다시 잰다.
  준비를 마친 뒤의 발들은 예전처럼 1 s(`braceTime`) 엎드렸다 쏘고, 쏜 뒤 5 s(`postFireLock`) 동안 움직이지 못한다.
  위험 인디케이터는 늘어나지 않는다 — 날아가는 포탄 하나에 하나 그대로다.
- **제 부대 재소환** (2026-09-18 신규 — 예전의 「평생 한 번 소환」을 대신한다): 이 포병에게 딸린 스캐빈저(굴착 호위 + 소환 무리)가
  **모두 죽으면** 25 s(`squadCooldown`) 뒤에 2~3마리(`summonMin/Max`)를 제 둘레에서 파낸다. 사거리 안에 사람 · 안드로이드 표적이
  있으면 그 표적에게 보내고(쓰러질 때까지 표적을 바꾸지 않는다), 없으면 굴착 호위처럼 곁을 지킨다. 종류는 `scavenger_summon`
  (드롭 0 % · 처치 경험치 0) 이라 되풀이 소환이 파밍이 되지 않는다.

## 6. 땅굴벌레 이벤트 (`src/enemies/sandworm`, 등장 판정 개편 2026-09-15)

- **등장 — 누적 확률제** (미리 굴리지 않고 시각 창도 없다, 레이드당 최대 1회): 호스트가 2 s 마다 조건을 보고 그때의 확률을 굴린다.
  - 후보 = 살아 있는 사람 + **안드로이드 분대원**. **자격 인원** = 달리는 중 + 무게 「조금 무거움」 이상. 자격 인원끼리 40 m 안에 **2명 이상**
    모여야 굴림이 있다 — **혼자(안드로이드 없는 솔로)는 절대 나오지 않는다.**
  - 검사 한 번의 p = 위협 배수(`SANDWORM_BASE_CHANCE_BY_THREAT` 0.6 / 0.8 / 1.0) × min(1, Σ light 0.12 · heavy/과적 0.25) × 거리 계수
    (서로 평균 20 m 이하 1 → 40 m 에서 0.25) + 유인 수류탄 0.15. 3명 heavy · 20 m 안 · 위협 3 = 0.75/검사, 2명 light · 40 m = 0.06/검사.
    표 전체는 `src/enemies/sandworm/Director.ts` 머리 주석.
  - 자리 = 무리 한가운데 (유인 수류탄이 있으면 60 % 로 그 자리) — `WorldRef.burrowGroundOk` (평평한 맨땅: 구조물 · 선로 · 흙길 · 소품 · 둥지 ·
    포자 군락 · 재해 구역 · 채집 · 상자 · 함선 외피 없음)를 지나야 한다. 무리 중 누가 데크 · 지붕 위면 건너뛴다.
  - **진동 장치**(가젯)가 다섯 번째로 두드리면 확률 없이 그 자리에서 곧장 전조 (`sandworm:summon`). 이미 나온 레이드에서는 무시.
- **종류**: 위협 1 → 어린 개체 `sandworm_weak`, 위협 2–3 → 성체 `sandworm`.
- **전조** 5 s: 분진 · 흙 파임 · 점점 강해지는 흔들림(반경 180 m) · 토스트.
- **분출**: 반경 12 m(어린 개체 8.4 m) · 55 피해(공용 2단 감쇠, 하한 30 %) · 넉백 15 m/s(베헤모스 돌진 12 보다 세다). 실드부터 깎인다. 동시에 6~14 m 링으로 벌레 무리가 파고 나온다 (어린 개체는 스캐빈저만).
- **뱉기 단계** 30 s: 다 솟아오르는 데 1.4 s, 그 뒤 4.5 s 마다 벌레 2마리(비행 1.1 s, 착지 7~16 m). 전체 적 생존 상한 48.
- **독극물 단계**: 땅에 박힌 채 48 m 안의 가장 가까운 플레이어에게 2.4 s 간격으로 산성 3발. 한 덩어리 피해는 스퓨어 산성과 같다.
- 성체: 몸통 반경 2.2 m · 높이 10 m, 입(머리) 반경 1.5 m(`headMul 1.5`). 어린 개체: 1.54 m · 7 m · 1.05 m. 경직 면역(`staggerFraction 99`), 질량 1000 이라 다른 벌레가 늘 밀려난다.

## 7. 행성별 구성

`data/planets.csv` 의 `bugs` 열 (`적종류:가중치`).

| 행성 | 위협 | 벌레 구성 | 비고 |
|---|---|---|---|
| 아켈론 II `amber` | 1 | 스캐빈저4 · 헌터2 · 워리어1 · 포격1 | 벌레가 드물다(`pressure 0.85`) |
| 보레아스 IX `tundra` | 2 | 스캐빈저3 · **헌터4** · 차저2 · 워리어2 | 무리 사냥꾼 |
| 베르단트 III `mossy` | 2 | 스캐빈저4 · **스퓨어3 · 독성3** · 워리어2 · 헌터1 | 산성 개체 위주 |
| 피로스 VII `ashen` | 3 | 스캐빈저3 · 워리어3 · **차저3 · 베헤모스1** · 포격2 | 중장갑 + 상시 포격 (`maxArtillery 3` · `maxBehemoth 2`) |
| 카민 I `crimson` | 3 | 스캐빈저2 · 헌터3 · 스퓨어2 · 워리어2 · 포격2 | 포그 없음 = 먼 거리 교전 |

위협도 배수(`data/tables.csv`):

- `BUG_HP_MUL_BY_THREAT` — 체력. `world:ready` 로 전달되어 `enemies/parts/Pool.acquire` 에서 적용된다 (와이어 없음).
- `BIG_BUG_WEIGHT_MUL_BY_THREAT` — 차저 · 베헤모스 · 포격 비중, 순찰 중형 슬롯 확률, 포병 굴착 확률.
- `MID_BUG_WEIGHT_MUL_BY_THREAT` — 워리어 · 스퓨어 비중 (땅굴벌레 뱉기 · 분출 무리도 같은 가중치).
- `PATROL_BEHEMOTH_BY_THREAT` · `ARTILLERY_CAP_BONUS_BY_THREAT` · `BEHEMOTH_CAP_BONUS_BY_THREAT`.

> **2026-09-18 — 행성 표본 채집지가 없어졌다** (`planets.csv` 의 `sampleNodes` 0). 이제 **미확인 세포는 벌레 시체와 벌레 알**에서 나온다
> (구조물 컨테이너 · 행성 광맥은 광물 · 유전자 쪽이다). 세포를 떨구는 벌레는 헌터 · 독성 60 % · 워리어 · 포병 확정 · 차저 1~2개 ·
> 베헤모스 2~3개 · 땅굴벌레 3~4개, 그리고 **벌레 알 80 %** 다 (`loot_corpse_samples.csv`, 개당 등급 굴림 — 이 표는 서사 이상 게이트에서 빠진다).
> 같은 날 **둥지 둘레의 상자 고리도 없어져** 레이드마다 3티어 상자 4~6개가 줄었다 — 그 자리를 대신하는 것이 둥지의 알이다.

## 8. 튜토리얼 전용

`world/tutorial` 의 고정 목록만 쓰고 본편 · 훈련장에는 서지 않는다. 수치는 스캐빈저 그대로이고
(`enemies/EnemyTypes.baseTypeOf` 가 리그 · AI · 소리를 스캐빈저로 돌린다) 다른 것은 고정 드롭 하나뿐이다.

| type | 바탕 | 차이 |
|---|---|---|
| `tut_bug` | 스캐빈저 | 없음 |
| `tut_bug_loot` | 스캐빈저 | 생체 조직 · 터미니드 분비선 100 % 확정 드롭 (`loot_corpses.csv` · `loot_corpse_rolls.csv` 의 `tut_*` 줄) |

튜토리얼에는 행성이 없어 `BUG_HP_MUL_BY_THREAT` 는 ×1 이다.

## 9. 벌레가 아닌 것

인간형 팩션은 벌레 취급이 아니다 — 목표 판정 축이 `bug` / `humanoid` 로 갈린다 (`src/meta/NpcRules.ts` `enemyMatches`).

- **안드로이드** `android` (위협 1), **로그** `rogue` · `rogue_boss` (위협 2), **레이더** `raider` (위협 2~3)
- **네임드** `rogue_sniper`(로든) · `rogue_hammer`(타길라) · `rogue_heavy`(헤비) — 레이드당 최대 1명
- **스캔 드론** `rogue_scan_drone` — 로든이 띄우는 공중 유닛, 요격 가능

코드에서 인간형 판정은 `isRogue` 가 아니라 `Enemy.isHumanoid` 를 쓴다.
